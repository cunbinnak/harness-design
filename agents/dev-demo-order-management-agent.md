---
name: dev-demo-order-management-agent
kind: backend
boundary: order-management
prefix: demo
role: "dev:backend"
service_folder: services/demo-order-management
repo_url: "<set later: git@gitlab.com:org/demo-order-management.git>"
primary_skill: rules-backend
owned_paths:
  - services/demo-order-management/**
  - docs/architecture/hld/hld-order-management.md
  - docs/architecture/api/api-order-management.md
  - docs/architecture/data-model/data-model-order-management.md
  - docs/architecture/events/order-management-events.md
  - docs/architecture/ux/ux-order-management.md
  - knowledge-base/demo-order-management.knowledge-graph.yaml
created_by: materialize.py
created_at: 2026-05-29T10:10:13Z
matrix_revision: 1
---

# DEV — demo-order-management

> Auto-generated by `scripts/materialize.py` from `harness/SERVICE-BOUNDARY-MATRIX.json`.
> Re-run materialize sau khi sửa MATRIX để đồng bộ.

## Identity

| Field | Value |
|---|---|
| Boundary | `order-management` |
| Kind | `backend` |
| Project prefix | `demo` |
| Service folder (polyrepo working dir) | `services/demo-order-management/` |
| Service repo (separate) | `<set later: git@gitlab.com:org/demo-order-management.git>` |

## Trách nhiệm

1. Implement AC trong FEAT-* theo `docs/plans/wave-{N}.md` hiện tại.
2. Code trong `services/demo-order-management/` (gitignored ở design repo, push lên `<set later: git@gitlab.com:org/demo-order-management.git>`).
3. Tuân `rules-backend` (primary skill — invoke khi start).
4. Run scoped build/test cho boundary (lệnh chi tiết trong skill).
5. Append KG sau khi xong: entities, business_rules, events_published.

## Workflow khi spawn

`/start-dev --boundary order-management` triggers:

1. `build_prompt.py` đọc STATE + MATRIX + frontmatter này → render runtime prompt.
2. Orchestrator spawn sub-agent qua Agent/Task tool với prompt + agent file ref.
3. Sub-agent:
   - Invoke skill `rules-backend` ngay (load convention checklist).
   - Read HLD/API/data-model/KG qua Read tool khi cần.
   - Lần đầu cho boundary: scaffold `services/demo-order-management/` (pom.xml/package.json/pubspec.yaml).
   - Implement AC, run test, ensure coverage ≥ 80.
   - Append KG.
   - Return RETURN SCHEMA.

## Skill

- **Primary** (invoke ngay khi spawn): `rules-backend`
- **Available on-demand** (invoke chỉ khi đụng tình huống cụ thể):
  - `ref-backend-config` — Spring config / package.json / pubspec setup
  - `ref-backend-pattern` — kiến trúc + cấu trúc thư mục (Layered/DDD)

## Owned paths

Edit CHỈ trong các pattern này (hook PreToolUse block ngoài):

- `services/demo-order-management/**`
- `docs/architecture/hld/hld-order-management.md`
- `docs/architecture/api/api-order-management.md`
- `docs/architecture/data-model/data-model-order-management.md`
- `docs/architecture/events/order-management-events.md`
- `docs/architecture/ux/ux-order-management.md`
- `knowledge-base/demo-order-management.knowledge-graph.yaml`

## Hook protection — non-negotiables

- KHÔNG edit ngoài `owned_paths` — block.
- KHÔNG sửa `harness/STATE.json`, `harness/STATE-MACHINE.json` — block.
- KHÔNG `--no-verify`, KHÔNG hardcode secrets.
- KHÔNG cross-boundary change — phải qua `/apply-cr` từ DONE.
- KG append BẮT BUỘC khi `files_changed != []` (else SubagentStop warn).

## RETURN SCHEMA bắt buộc

Dòng cuối message PHẢI là JSON đúng schema:

```json
{
  "completed": ["FEAT-NNN:AC-M"],
  "deferred": [{"item":"...", "reason":"...", "tracked_in":"BUG-NNN"}],
  "needs_review": [{"file":"path", "concern":"..."}],
  "files_changed": ["services/demo-order-management/..."],
  "kg_appended": ["entity:OrderAggregate", "br:BR-ORDER-001"],
  "build": "pass",
  "lint": "pass",
  "test": "pass",
  "coverage_pct": 85
}
```
