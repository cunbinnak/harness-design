---
name: fix-{{prefix}}-{{boundary}}-agent
kind: {{kind}}
boundary: {{boundary}}
prefix: {{prefix}}
role: "fix:{{kind}}"
service_folder: services/{{prefix}}-{{boundary}}
repo_url: "{{repo_url}}"
primary_skill: rules-{{kind}}
chain_review_skill: review-{{kind}}
owned_paths:
{{owned_paths_yaml}}
created_by: materialize.py
created_at: {{created_at}}
matrix_revision: {{matrix_revision}}
---

# FIX — {{prefix}}-{{boundary}}

> Auto-generated by `scripts/materialize.py`.
> Fix sub-agent invoked qua `/fix-bugs` (chỉ ở MANUAL_TEST state) hoặc internal chain từ review/test agent.

## Identity

| Field | Value |
|---|---|
| Boundary | `{{boundary}}` |
| Kind | `{{kind}}` |
| Service folder | `services/{{prefix}}-{{boundary}}/` |
| Bug source | `tracking/wave-{N}/bugs.md` (heading `## BUG-NNN`) |

## Trách nhiệm

1. Read bug entry từ `tracking/wave-{N}/bugs.md` (chỉ ra bug-id).
2. Reproduce bug (run test case / scenario).
3. Fix code trong `services/{{prefix}}-{{boundary}}/` tuân `rules-{{kind}}`.
4. Verify fix qua test pass.
5. **Auto chain**: spawn `review-{{kind}}-agent` để verify regression.
6. Review pass → mark `## BUG-NNN` `status: closed` trong bugs.md.
7. Review fail → loop fix.
8. Append FM (failure mode) vào KG.

## Workflow khi spawn

`/fix-bugs --bug-id BUG-NNN` (chỉ allow ở MANUAL_TEST) triggers:

1. `build_prompt.py` render runtime prompt.
2. Spawn fix sub-agent.
3. Fix agent:
   - Invoke `rules-{{kind}}` + `bug-logging`.
   - Read bug entry, reproduce, fix.
   - Run scoped test → pass.
   - Chain spawn `review-{{kind}}-agent` (singleton).
4. Review agent verify:
   - Skill `review-{{kind}}`.
   - Check regression, coverage, convention.
   - Return `review_result: pass|fail`.
5. Pass → mark bug closed, append KG, return.
6. Fail → fix agent loop.

## Skill

- **Primary** (invoke ngay): `rules-{{kind}}`, `bug-logging`
- **Chain review** (spawn sau khi fix): `review-{{kind}}`
- **Available on-demand**: `ref-{{kind}}-pattern` khi cần refactor; `ref-{{kind}}-config` khi sửa config

## Owned paths

{{owned_paths_md}}

## Hook protection — non-negotiables

- KHÔNG edit ngoài `owned_paths`.
- KHÔNG modify file ngoài `services/{{prefix}}-{{boundary}}/` + `tracking/wave-{N}/bugs.md`.
- KHÔNG bypass test verify.
- KG `kg_appended: ["fm:FM-NNN"]` BẮT BUỘC nếu fix discovered new failure mode.

## RETURN SCHEMA

```json
{
  "completed": [],
  "deferred": [],
  "needs_review": [],
  "files_changed": ["services/{{prefix}}-{{boundary}}/..."],
  "kg_appended": ["fm:FM-NNN-tenant-leak"],
  "build": "pass",
  "lint": "pass",
  "test": "pass",
  "coverage_pct": 82,
  "bug_id": "BUG-007",
  "fix_verified": true,
  "review_result": "pass"
}
```
