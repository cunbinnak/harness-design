# prepare-dev

Điền **§2 Assignment** trong `docs/plans/waves/{wave-id}/wave.md` (file gộp plan + assignment).

Evidence: `features_in_flight`, `boundaries_in_flight`.

```bash
python scripts/build_command_prompt.py prepare-dev
```
