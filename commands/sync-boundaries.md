# sync-boundaries (đã gộp vào start-wave)

Tự chạy khi **`start-wave complete`** (`run_sync_matrix` trong harness).

Roster → `harness/SERVICE-BOUNDARY-MATRIX.json`.

Dùng: [start-wave.md](start-wave.md)
