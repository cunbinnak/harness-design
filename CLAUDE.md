# Orchestrator (Claude / Cursor / CLI)

**Guide:** [`SETUP-GUIDE.md`](SETUP-GUIDE.md)

Advance the harness one command at a time: `py scripts/harness.py <command> complete` only when `workflow.allowed_next` allows it. Use `py scripts/harness.py state` to inspect the current step.
