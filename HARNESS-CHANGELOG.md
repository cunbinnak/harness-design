# Harness Change Log

> Nhật ký thay đổi kernel/gate/skill của ADLC Design Harness (append-only). Tách khỏi CLAUDE.md để router gọn — CLAUDE.md chỉ giữ trạng thái HIỆN TẠI, file này giữ LỊCH SỬ.
> Mỗi thay đổi non-trivial (gate mới, đổi flow, vá bug enforcement) thêm 1 row `| Date | CR/ADR | Summary |` ở CUỐI. Mới nhất ở dưới cùng.

| Date | CR/ADR | Summary |
|---|---|---|
| 2026-05-29 | — | Initial CLAUDE.md (router style v4 rebuild) — polyrepo, 10 states, 13 commands |
| 2026-06-13 | — | Clone DISCOVERY (D0-D3 ideation) từ ADLC: +4 state `DISC_D0..D3`, +2 command `/discovery-start`,`/discovery-end`, +gate `discovery_gate.py`, +docs/discovery/*. |
| 2026-06-13 | — | Tách intake hoàn toàn (bỏ `/intake-requirement`), khớp cách chia nhỏ của ADLC: front-half = DISCOVERY(D0-D3) → DOMAIN_AUTHORING → DESIGN → PLAN → REVIEW (rename từ INTAKE). DOMAIN author Epic/Feature/BR **thẳng vào `docs/architecture/{epics,feat,business-rules}`** (1 stage, no translate/sync/docs-domain — đó là plumbing multi-repo); skill domain-po (EPIC/FEATURE) + domain-ba (BR) + agent; template ZIP. DESIGN=`/design` (technical-design), PLAN=`/plan` (implementation-plan) tách stage riêng, gate force-capable (design_gate/plan_gate). MATRIX materialize ở PLAN (ALLOW_STAGES). apply-cr → DESIGN. |
| 2026-06-13 | — | **Audit sync ADLC→harness (fix drift)**: sửa số liệu IDENTITY (10→17 states, 14→19 commands); cập nhật tham chiếu chết (intake step3/4, `/intake-requirement`) → DESIGN / Discovery D3; bổ sung mode `JOURNEY`+`PERSONA` vào chữ ký `/domain-start` ở CLAUDE.md + đồng bộ `commands/domain-start.md` (frontmatter, bước 1&5, dòng cuối); bỏ `WIREFRAME` (mode đã xoá) và `DOMAIN_TRANSLATING`/`TRANSLATE` (plumbing multi-repo đã bỏ) trong message "Sau D3" của `build_prompt.py`. |
| 2026-06-13 | — | **Quyết định: KHÔNG clone các tính năng ADLC sau** (single-repo nên không cần / hoãn): SPECS hub + translate/sync + ORCHESTRATOR UI + repo UIUX/BOUNDARIES/EXPERIENCES tách rời (đã bỏ có chủ đích từ đầu). **Hoãn (gap đã nhận diện, chưa làm)**: (1) state `BLOCKED` + `/blocker-raise`/`/blocker-resolve` (escape hatch khi agent kẹt); (2) design-system/design-tokens/component-library dùng chung (hiện chỉ UX per-boundary qua `ux-design`); (3) `/cr-raise` giữa luồng (hiện chỉ `/apply-cr` từ DONE); (4) `/import-baseline` brownfield Wave 0; (5) qa-translator re-map TC khi AC đổi + dedupe-registry; (6) governance util `/decision-log`,`/memory-audit`,`/reset-question-budget`,`/scope-extend`; (7) doc tổng quan system-level (SYSTEM-ARCHITECTURE/SYSTEM-TOPOLOGY/CONTRACT-MAP) — hiện rải ở HLD per-boundary + BOUNDARY-MAP. Cân nhắc bổ sung khi có nhu cầu thực tế. |
| 2026-06-14 | — | **DESIGN tách self-loop + `/design-end`** (19→20 command): `/design` giờ `DESIGN→DESIGN` (thảo luận/refine, KHÔNG advance), `/design-end` `DESIGN→PLAN` (verify gate, không spawn author mới); per-role docs-to-read tối ưu. **3 gate single-repo** chắt lọc ZIP per-target MANIFEST: `design_gate` per-boundary completeness (backend/bff→hld+api, web/mobile→hld+ux), `plan_integrity` (FEAT-id MATRIX có file + depends_on no-cycle/no-dangling), `matrix_coherence` (MATRIX phủ đủ boundary đúng kind) + `planning_lint` ref-integrity mở rộng journey/persona, persona-ref chuẩn-hoá file-id `PERSONA-{prefix}-NNN` (bỏ P-id pool). **23 template** chuẩn-hoá theo ZIP rồi trim lean. |
| 2026-06-14 | — | **`apply-cr` re-enter `DOMAIN_AUTHORING`** (was DESIGN) — vá gap mở-rộng-feature-sau-ship: CR thêm feature → `/domain-start` author epic/feat/BR rồi chảy DOMAIN→DESIGN→PLAN→REVIEW→start-wave; CR kiến trúc-only → `/domain-end` qua thẳng. Boundary MỚI vẫn dùng done-wave→`/discovery-start D3` (discovery re-runnable idempotent vì done-wave giữ docs). smoke +1 step (apply-cr→DOMAIN). |
| 2026-06-14 | backlog G12+G13+G1 | **Back-half honest-gate (rút từ e2e ClinicBook — META "giàu spec nghèo enforcement máy"):** (G13) `scripts/capture_infra_proof.py` HARNESS tự curl `/health/ready` → `health-proof.json` + gate `health_proof` @dev-handoff/test-plan (State=running chưa đủ; app phải trả 2xx); (G12) gate `test_evidence` @test-execute parse test-report+test-logs (auto-TC in-scope phải có network-call thật, cấm silent-skip) + **DERIVE `test_result`** từ report (không tin agent tự khai); (G1) deferred-scope: `wave-{N}.md §6 Deferred` là SoT → test-plan tag `@deferred` → test-execute skip(deferred) không log bug → `/end-wave` close sạch tự nhiên, hết ép `test_result=pass`. Tag không khai báo wave plan → coi in-scope (chống né test). Gate selftest hermetic + smoke 30/30. |
| 2026-06-16 | nhận-xét #2+#3+#9 | **DOMAIN 2-lớp business↔eng + KÝ + TRANSLATE (clone ZIP, adapt single-repo) + E-6 chặt.** (A1) lớp BUSINESS plain VN `docs/domain/{epics,feat,journeys,business-rules,personas}` (template clone ZIP, có "Câu hỏi cho Author") — `/domain-po`(EPIC/FEATURE/JOURNEY) + `/domain-ba`(BR/PERSONA) author (loop tới khi OK + hỏi-author-questions ngay, status DRAFT). (#3) `/domain-approve <id|all>` KÝ (`status: APPROVED`, ký TRƯỚC dịch SAU) — `domain_approve.py` jargon-check + stamp; gate `domain_no_jargon`. (#2) `/domain-translate` spawn `domain-translator` (clone ZIP agent: DỊCH-KHÔNG-sáng-tác, field kỹ thuật = TODO-engineer) dịch `docs/domain/`→`docs/architecture/` eng; gate `domain_signed` (mọi doc ký). Bỏ SPECS-hub/cross-repo-sync. command 20→23 (bỏ `domain-start`, +`domain-po`/`-ba`/`-approve`/`-translate`); back-edge DESIGN→DOMAIN qua po/ba; phase-lock +`docs/domain/`. (#9/E-6) hook chặn spawn tự-viết-tay MỌI workflow agent: detect theo **registry tên-agent** (`agents/*.md`) + keyword → thiếu chữ ký build_prompt = block. gates+policies selftest + smoke 35/35 + state validate + parity xanh. |
| 2026-06-16 | nhận-xét #1 | **Discovery: gộp cơ chế start D0→Dn + complete-before-spawn.** `/discovery-start` giờ TIẾN qua wave (nhảy tiến `D{N}→D{N+1}` = gate wave hiện tại qua `discovery_advance`; gọi lại cùng wave = refine, no gate); bỏ 3 transition `discovery-end` tiến-wave, `/discovery-end` chỉ còn 1 (DISC_D3→DOMAIN, không arg). **complete-before-spawn**: command transition STATE NGAY (trước build_prompt+spawn) → STATE đúng stage để phase-lock cho agent ghi `docs/discovery/**` + next-step/Stop-hook đúng (giống `/start-dev`). `find_transition` thành **evidence-aware** (phân biệt refine vs advance cùng `(from,trigger)`). gates selftest + smoke **32/32** + state validate + parity xanh. |
| 2026-06-16 | nhận-xét #6+#7+#8+#10+#11+#12+#13+#14 | **Batch dev-quality + flow-control (từ chạy thật tới test-execute):** (#6) entity Java → `{Resource}Entity` + folder `entities/` (rules-backend + ref-backend-pattern). (#7) **Gradle default** mọi backend (`build.gradle` Groovy DSL; Maven opt-in qua ADR) — rules/ref/review/template + dispatcher `_build_test_cmd` Gradle-first. (#8) review-backend §L2 ép check tuân-thủ `rules-backend` + ref-skill (cấm **FQCN** inline — phải import) + §N schema-drift (entity↔migration qua Testcontainers `ddl-auto:validate`). (#10) `/review-dev complete{}` ép `review_results` (gate `non_empty`) chống STATE rỗng kẹt dev-handoff; `all_boundaries_reviewed` thêm force-bypass. (#11) **turn-flag**: 1 `harness complete`/user-turn (marker `harness/.turn-advance.flag`, reset UserPromptSubmit/SessionStart) — complete thứ 2 deny "MAIN tự nối lệnh"; gate-fail KHÔNG tiêu cờ. (#12) dev-handoff **INFRA-ONLY**: `_pre_task` set `spawn.active=dev-handoff-agent` → `PreToolUse(Write\|Edit)` block `services/**` (FM-HANDOFF-NO-CODE-FIX) — lỗi code boundary → fix-agent, không tự vá. (#13) test-plan + test-execute trỏ template `tracking/_templates/TEMPLATE.test-case-registry.md`. (#14) test-execute SEED test-data (cột `test-data`/`pre-condition` đủ cụ thể) trước chạy + cleanup. gates+policies selftest + smoke 35/35 + parity. |
| 2026-06-21 | service-lifecycle wave | **End-wave TẮT service + handoff reuse rõ ràng.** (1) `/end-wave` thêm `docker compose stop` (dừng container, GIỮ image+volume) — UAT đã xong ở MANUAL_TEST (handoff §5 sửa nhãn "sau end-wave"→"ở MANUAL_TEST trước end-wave") nên không cần service chạy; trước đây giữ UP "post-mortem" (lý do yếu). KHÔNG `down --volumes` (vẫn là việc `/done-wave`). build_end_wave + end-wave-agent (+skill infra-local-dev, +`infra_stopped`) + command + handoff §8. (2) Handoff wave kế **tái dùng** service wave trước: `docker compose up -d --build` idempotent — image+volume từ `end-wave stop` giữ nguyên → khởi động NHANH, chỉ boundary mới/đổi mới build; KHÔNG `down` trước (ghi rõ ở build_dev_handoff). smoke 35/35 + selftest + parity. |
| 2026-06-21 | turn-flag auto-mode hole | **Vá MAIN tự nối slash-command ở auto mode.** Lỗ hổng: turn-flag (#11) reset ở `UserPromptSubmit`; khi MAIN TỰ invoke slash-command kế (Skill/SlashCommand tool) để chạy `/test-plan`/`/test-execute`… → fire lại `UserPromptSubmit` → xoá cờ → `harness complete` kế lọt → nối trọn pipeline (auto mode không có ma sát phê duyệt). Fix: thêm `PreToolUse(Skill\|SlashCommand)` hook (`dispatcher._pre_skill`) chặn MAIN gọi tool chạy lệnh ∈ `GATE_RULES` (deny tại NGUỒN, trước khi slash-command reset cờ); lệnh user GÕ tay pre-loaded → không qua tool → không ảnh hưởng; skill ngoài-harness cho qua. +matcher `Skill\|SlashCommand` ở `.claude/settings.json`. dispatcher selftest + gates/policies selftest + smoke 35/35 + parity. |
| 2026-06-21 | backend-lombok-consistency | **Vá mâu thuẫn ref backend ↔ hub (dev+review hiểu sai `@RequiredArgsConstructor`/`@Slf4j`):** `rules-backend` (hub) ĐÚNG bắt `@RequiredArgsConstructor` + `@Slf4j`, nhưng VÍ DỤ CODE ở ref skills lại viết NGƯỢC (constructor tay `Impl(...){this.x=x;}` + `LoggerFactory.getLogger(...)` thủ công) → dev copy ref → sai, review không có mục bắt → lọt. Sửa ví dụ cho khớp hub ở `ref-backend-pattern` (service), `ref-backend-logging` (`@Slf4j`, bỏ LoggerFactory), `ref-backend-redis` (RedisServiceImpl), `ref-backend-kafka` (4 bean: publisher/command-service/relay/consumer); thêm forbidden `LoggerFactory.getLogger` thủ công. `review-backend §L2` thêm mục bắt **DI manual-constructor/`@Autowired`-field + manual-LoggerFactory = MAJOR**. Constructor tay hợp lệ giữ nguyên (enum `OrderErrorCode`, exception `BusinessException`); component stateless (interceptor/mapper) không cần annotation. |
| 2026-06-21 | E-6 test-spawn | **E-6 keyword phủ test-plan/test-execute (vá MAIN tự build prompt spawn test).** `DEV_SPAWN_KEYWORDS` += `test-plan`, `test-execute` → `PreToolUse(Task)` block spawn 2 agent này bằng prompt tự-viết-tay (trước đây chỉ nhánh tên-agent `test-*-agent` chặn → MAIN né bằng cách không nhắc tên agent). build_prompt output (`# SPAWN PROMPT`) vẫn pass; space-form "test plan" KHÔNG false-positive. policies selftest + smoke 35/35 + parity. |
| 2026-06-16 | nhận-xét #15 | **Gate `doc_review` @ `/approve-document` — doc-review 2 mode.** `/review-document` **CÓ arg** = revision (sửa theo comment user, vòng đã có); **KHÔNG arg** = sanity-check soi 5 lens (độ-phủ năng-lực `capability-map`+persona+journey→FEAT; mâu-thuẫn cross-doc; AC testable; cross-ref; câu-hỏi-author chưa chốt) → ghi `tracking/doc-review-findings.md` (`DR-NNN` + severity, template `tracking/_templates/`). Gate `check_doc_review`: thiếu file (chưa chạy sanity-check) HOẶC còn gap **BLOCKER/MAJOR** open → **chặn approve** (mirror `review-dev no_open_findings` cho TÀI LIỆU; bắt **thiếu năng lực nền vd auth/login** trước build). Lens đặt ở build_prompt + `review-document-agent` (KHÔNG đụng skill `business-analysis`). force-bypass+audit `decisions.md`. gates selftest +doc_review hermetic + smoke 35/35 + parity. |
| 2026-06-14 | — | **Doc phase-lock + back-edges + next-step hint (vá 2 gap user: hook hổng + báo-bước-tiếp mơ hồ):** (#2) port ZIP `pretooluse-readonly-inputs.py` sang single-repo = `policies.phase_lock_violation` ở `PreToolUse(Write|Edit)` — doc discovery/domain/design/plan chỉ sửa ở stage sở hữu (+REVIEW), stage sau frozen (NON-NEGOTIABLE #6 từ honor-system → hook-enforced; chống dev/test sửa spec khớp code + sửa FEAT/HLD lúc ở PLAN). Sửa upstream = **back-edge** `PLAN--/design-->DESIGN`, `DESIGN--/domain-start-->DOMAIN` (dùng lại lệnh entry, không command mới → parity giữ; tiến lại re-gate) hoặc `/apply-cr` sau ship. (#1) next-step hint contextual (`policies.STAGE_NEXT_GUIDE`: lệnh+arg+nghĩa+back-edge) thay list tên trống. policies selftest + smoke 32/32 (+2 back-edge) + state validate + parity. ZIP translate (plain↔eng DOMAIN→SPECS) giữ nguyên BỎ (single-repo author thẳng). |
| 2026-06-14 | backlog G15-now | **UI design-fidelity deterministic (e2e obs #6, Figma defer):** artifact dùng chung `docs/architecture/ux/design-tokens.css` (SoT `--color-*`/`--font-*`/`--space-*` + dark/hc theme, `TEMPLATE.design-tokens.css`) — mọi web boundary consume, không bịa palette per-boundary; nâng gate `web_styling`: web style bằng plain CSS phải dùng design token `var(--...)` (tailwind/CSS-in-JS miễn) → chống "FE bịa màu/spacing rời design system". Skill ux-design + rules-web (rule 44) trỏ shared token. G15-future (Figma design-to-code + visual diff) DEFER. |
| 2026-06-14 | backlog G16 | **Wave-sequence validator (e2e obs #7 + #5 verify):** port `scripts/wave_sequence_lint.py` từ ZIP `wave-sequence-validate.py` (single-repo: bỏ contract-signing, `inherited_active`→file-exists) → gate `wave_sequence_lint` @plan: enum `wave_class`/`wave_strategy` + `target_count_per_layer ≤ 3` + strategy layer-purity (horizontal-be/-fe) + vertical `parent_epic` + `inherited_active` file tồn tại. Field §2 WAVE-SEQUENCE từ "trang trí" → được gate; bỏ chữ "chưa gate"/"forward-looking" ở template + implementation-plan SKILL. Selftest hermetic (module + wiring) + smoke 30/30 + parity. **+ #5 verify doc-contract:** thêm invariant ZIP `lint_execution` "FAIL phải log bug" vào `test_evidence`; deferred SoT 1 nguồn (handoff §6 → wave §6). |
| 2026-06-14 | backlog G11+G4/G6 | **Dev-quality + integration-realism gate (e2e obs #1,#2):** (G11) gate `code_compliance` @dev-handoff — backend boundary cấm H2 (`com.h2database`/`jdbc:h2`/`ddl-auto: create-drop`) + bắt `Dockerfile` + `application.{yml,properties}` (đối xứng `web_styling`, chặn "test xanh nhờ H2" + "dev-done ≠ runnable"); (G4/G6-A) gate `contract_test_present` @test-plan — consumer (có `depends_on` trong wave) phải có ≥1 auto-TC contract/integration/e2e nối tới (chống thiếu liên kết BE-FE → bug); (G6-B) gate `api_transport` @plan — api spec KHÔNG truyền tenant-id qua query string (phải `X-Tenant-ID` header/JWT claim, api template §2 — chống drift BUG-012). Tất cả force-bypass+audit; selftest hermetic + smoke 30/30. |
| 2026-07-04 | EXAMPLE → thư viện ARCHETYPE màn (generic mọi đề bài) | **Trả lời "example gắn 1 đề bài, đề khác theo sao?" (user):** cái cần neo không phải màn cụ thể mà là ARCHETYPE — app nghiệp vụ nào cũng ghép từ bộ hữu hạn. Viết lại `EXAMPLE.reference.html` thành gallery 6 archetype nội dung generic: 1 Dashboard (KPI cards) · 2 Bảng danh sách (toolbar+hover-row+badge+pagination) · 3 Form (2 cột→1 cột, lỗi cạnh field, actions phải) · 4 Trang chi tiết (header+badge+description list) · 5 Timeline/lịch tài nguyên (block span, ô trống im lặng) · 6 Feedback states (skeleton/empty/error-banner/toast/modal). Mỗi màn tương lai: xác định archetype (hoặc ghép) → so section đó. Skill/spawn-prompt/README cập nhật wording. |
| 2026-07-04 | EXAMPLE exemplar + luật màu neutral-first | **Root-cause "demo đẹp — chạy thật vẫn xấu" vòng 2 (screenshot: nền xanh pastel toàn màn, block hồng-đỏ):** chẩn đoán — (a) taste không truyền được bằng tính từ, LLM bắt chước VÍ DỤ tốt hơn nghe mô tả; (b) danh sách anti-pattern là đập chuột (agent sửa đúng 5 lỗi bị cấm, phạm lỗi mới); (c) có token nhưng thiếu nguyên tắc DÙNG màu. Vá: (1) **`ux/mockups/EXAMPLE.reference.html`** — bài mẫu chất lượng commit vào repo (exemplar đối chiếu, KHÔNG phải template điền; có :root fallback chỉ riêng file này để mở xem được trước khi project có design-tokens); skill/spawn-prompt BƯỚC 0 = mở bài mẫu, vẽ xong TỰ SO — thua = làm lại không trình user; (2) **LUẬT MÀU neutral-first**: nền luôn surface trung tính, CẤM sơn semantic lên mảng lớn (màu = nhấn ~10%), ô trống im lặng hover mới hiện affordance, booking = primary-soft (danger CHỈ cho lỗi); (3) phase-lock exempt `EXAMPLE.*` (policies + selftest). |
| 2026-07-04 | chuẩn chuyên nghiệp UI + ui-kit library | **Vá "demo đẹp mà chạy thật xấu hoắc" (screenshot user: bảng text-link 'Trống'×50 ô, link gạch chân thay button, booking không span):** (1) ux-design +§Chuẩn chuyên nghiệp & ANTI-PATTERNS đích danh (benchmark SaaS thương mại; cấm text-link lặp mọi ô — ô trống im lặng chỉ hover mới hiện affordance, cấm link thay button, booking = block span theo thời lượng, cấm bảng HTML mộc, lỗi/toast đúng chỗ, responsive kiểm thật); build_design_ux nhấn "phạm anti-pattern = làm lại, không trình user". (2) **ADR ui-kit chốt component library trưởng thành** (React → khuyến nghị Ant Design 5): mockup MÔ PHỎNG visual language của library (HTML tĩnh không nhúng antd được), dev dùng library THẬT + design-tokens map qua theme (ConfigProvider) — mockup↔app hội tụ; technical-design (ADR bắt buộc khi có FE) + rules-web 2 nhánh (library vs plain-CSS) + review-web check đúng ui-kit. (3) gates `_web_styling_signals` +`_UI_LIB_MARKERS` (antd/@mui/chakra/primereact): app dùng component library = styled hợp lệ + miễn var(--...) như tailwind (trước bị chặn oan). Selftest +antd case; smoke 36/36. |
| 2026-07-04 | màn tuân thủ FEAT (SCREEN-MAP ↔ feat 2 chiều) | **Design theo màn nhưng FEAT là khuôn (góp ý user):** `_screen_map_problems` +2 check khi `docs/architecture/feat/` đã author — (1) FEAT-id trong cột `feat` của row phải có file thật (màn phục vụ feature ma = chặn); (2) chiều ngược: MỌI FEAT `has_ui_touchpoint: true` (opt-out status dùng CHUNG `planning_lint._FEAT_OPTOUT_STATUSES` — không chép tay) phải xuất hiện ở ≥1 row (feature có UI mà 0 màn = bị bỏ rơi khỏi design = chặn). Hermetic selftest +3 case. Cùng họ traceability với ac_coverage (AC↔TC) nhưng cho lớp DESIGN (FEAT↔màn). |
| 2026-07-04 | thiết kế theo MÀN + SCREEN-MAP | **Đơn vị thiết kế = MÀN, không phải boundary (góp ý user):** nhiều FE boundary thì phải gắn RÕ màn nào thuộc boundary nào + đọc tài liệu nào. Thêm **`ux/SCREEN-MAP.md`** (ux-designer sinh TRƯỚC khi vẽ): bảng `screen|route|boundary|feat|mockup|note` — luật gán màn→boundary: FEAT `target_experience_hint`/`target_boundary_hint` → persona của experience (BOUNDARY-MAP/CHARTER) → mơ hồ HỎI user (không đoán); mỗi FEAT has_ui_touchpoint ≥1 màn; màn dùng chung = 1 row nhiều FEAT. Khi vẽ 1 màn chỉ đọc tài liệu CỦA MÀN đó (FEAT:AC + api mà flow gọi). `design_gate` thay check per-boundary-dir bằng **parse SCREEN-MAP từng row** (`_screen_map_problems`, hermetic selftest): mockup phải TỒN TẠI + dùng token; màn gán boundary ma = chặn; web boundary 0 màn = chặn ("boundary trắng design"). ux-designer own +SCREEN-MAP.md; reset script dọn; routing table +3 câu hỏi màn/mockup. |
| 2026-07-04 | dọn template UX cũ (bỏ TEMPLATE.ux.md) | **Nhất quán hướng "thiết kế thẳng": xóa `TEMPLATE.ux.md`** (di sản thời wireframe — §4 bảng token còn CHÉP GIÁ TRỊ trùng design-tokens.css = drift magnet). `ux-{boundary}.md` giờ viết thẳng theo **outline trong skill ux-design** (Tổng quan · Flows · Screens[link mockup+states+API+validation] · Permission · Global patterns · A11y · Edge cases · Open questions), luật mới: **KHÔNG chép giá trị token vào .md** — design-tokens.css là SoT duy nhất. GIỮ `TEMPLATE.design-tokens.css` (không phải khung điền mà là palette seed — asset của hướng mới). Cập nhật refs: skill/agent/build_prompt/docs README. |
| 2026-07-04 | mockup không template — thiết kế thẳng | **Bỏ `mockups/TEMPLATE.mockup.html`** (user: "không cần template, thiết kế luôn giao diện bằng HTML"): skeleton copy-điền gò bó — ux-designer giờ **THIẾT KẾ THẲNG giao diện hoàn chỉnh** per screen như trang web thật (tự dựng app shell + nội dung thật theo §Visual polish). LUẬT giữ nguyên và chuyển về `ux/mockups/README.md` (HTML tĩnh file:// · chỉ `var(--...)` — thiếu token thì THÊM vào design-tokens.css chứ không bịa tại chỗ · không lorem · state phụ = section · responsive · đủ hover/focus). Gate `design_gate` không đổi (≥1 mockup/web boundary + dùng token). Cập nhật: skill ux-design, ux-designer-agent, build_design_ux, command design-ux, TEMPLATE.ux note, gates message, reset script (giữ README khi dọn). |
| 2026-07-04 | design bằng HTML (mockup thay ASCII) | **`/design-ux` chuyển từ ASCII wireframe sang MOCKUP HTML TĨNH** (user chê "vẽ xấu quá trời" — ASCII không duyệt look được): mỗi screen 1 file `ux/mockups/{boundary}/{screen}.html` theo `mockups/TEMPLATE.mockup.html` mới (app shell compose từ design token, mở `file://` xem được, KHÔNG JS/build/CDN, chỉ `var(--...)`, state phụ loading/empty/error = section, responsive media query, nội dung thật không lorem). Phân vai SoT: mockup = LOOK, `ux-{boundary}.md` = BEHAVIOR (bỏ hẳn wireframe ASCII khỏi TEMPLATE.ux). `design_gate` +2 check: mỗi web boundary ≥1 mockup non-TEMPLATE + mockup phải reference token (`_web_mockup_problem`, hermetic selftest). Dev FE (rules-web rule 1) bám mockup làm SoT look; review-web §6 đối chiếu app↔mockup (lệch rõ = MAJOR); reset script dọn `mockups/{boundary}/`. ux-designer-agent own `ux/mockups/**`. smoke 36/36. |
| 2026-07-04 | doc_stamped (status máy-hóa @approve-document) | **Vá "approve document rồi mà doc vẫn DRAFT" (user bắt được khi chạy thử):** `/approve-document` trước đây CHỈ set `approved: true` vào STATE — frontmatter `status:` của doc design/contract là field trang trí không ai flip (bệnh field-không-máy-hóa như wave_class trước G16). Thêm `scripts/approve_document.py` (idempotent): adr/hld/data-model/ux/integrations → `status: APPROVED`; api/events (contract lifecycle) → `status: ACTIVE` (DEPRECATED giữ nguyên); KHÔNG đụng docs/domain (ký riêng) + eng product (TRANSLATED→ENRICHED) + plans (PLANNED = đúng nghĩa sau duyệt). Gate mới **`doc_stamped`** @approve-document verify stamp đã xảy ra trên disk (mirror domain_stamped; TEMPLATE/README miễn; vacuous khi chưa có doc; force+audit). gates+approve_document selftest + smoke 36/36. |
| 2026-07-04 | domain_stamped (vá approve chay) | **Vá "approve rồi vẫn DRAFT" (bắt được khi user chạy thử):** 2 nguyên nhân — (1) tài liệu nói stamp `approved: true` trong khi script/gate dùng `status: APPROVED` (drift chữ nghĩa 10 file: command/skill/agent/build_prompt/state.py/STATE-MACHINE) → thống nhất hết về `status: APPROVED`; (2) gate `/domain-approve` chỉ check jargon, KHÔNG verify stamp đã xảy ra → MAIN complete được mà quên chạy `domain_approve.py` (file vẫn DRAFT, sau đó `domain_signed` fail khó hiểu). Thêm gate **`domain_stamped`**: doc thuộc target phải ĐÃ `status: APPROVED` trên disk lúc complete (target lẻ chỉ check doc đó; chưa author gì = vacuous; force+audit); build_prompt/command nhấn "BẮT BUỘC chạy script, không stamp tay". gates selftest (+4 case) + smoke 36/36. |
| 2026-07-04 | tách /design-ux (ux-designer) | **UX/UI tách khỏi solution-architect thành bước + agent chuyên môn riêng (mirror pattern PO/BA của DOMAIN):** lệnh mới `/design-ux` (DESIGN→DESIGN self-loop + back-edge PLAN→DESIGN) spawn `ux-designer-agent` (skill `ux-design` có sẵn) — own `ux/ux-*.md` + `ux/design-tokens.css`; solution-architect GỠ quyền UX (owned paths + forbidden + workflow: FE boundary chỉ HLD + đảm bảo BE contract đủ cho UX consume). Thứ tự: `/design` (chốt boundary + api) → `/design-ux` (consume contract, không bịa endpoint) → lặp xen kẽ → `/design-end` (gate không đổi — per-boundary ux + design-tokens vẫn đòi đủ). 23→24 command; +2 transition STATE-MACHINE; build_prompt +build_design_ux; E-6 keyword +design-ux; STAGE_NEXT_GUIDE DESIGN/PLAN cập nhật; technical-design + ux-design skill đổi mô tả vai; đồng bộ README/AGENTS/SETUP-GUIDE/PROTOCOL/commands+agents README. smoke +1 step (36/36). Trả lời câu hỏi PO/BA: đã tách chuẩn từ trước (domain-po/domain-ba agent + skill + owned paths riêng) — UX là chỗ gộp vai cuối cùng. |
| 2026-07-04 | doc-align flow PO/BA | **Đồng bộ TOÀN BỘ tài liệu theo flow sản xuất mới (quét stale 1 lượt):** `/domain-start` (đã bỏ) + "author thẳng vào docs/architecture, KHÔNG translate" (flow cũ) còn rải ở ~30 chỗ → thay bằng chuỗi `/domain-po`·`/domain-ba` → `/domain-approve` → `/domain-translate`. Sửa: policies (STAGE_NEXT_GUIDE + _BACK_HINT + selftest), gates/build_prompt/discovery_gate messages, STATE-MACHINE descriptions (DOMAIN 2 lớp + discovery tiến-wave qua start), 6 agent + agents/README (22 agent, +domain-translator row, apply-cr→DOMAIN), 7 command + commands/README (bảng 23 lệnh + design self-loop/design-end + back-edge), 4 skill discovery (Done → `/discovery-start D{N+1}` thay `/discovery-end <D>` đã bỏ arg) + boundary-charter + business-analysis, README/AGENTS.md/SETUP-GUIDE (23 command, flow 2 lớp, gate checklist mới), PROTOCOL diagram, TEMPLATE.CHARTER/cr/doc-review-findings/api (§7 breaking), done-wave-agent allowed-next (apply-cr chạy TỪ DONE trước done-wave; start-wave chỉ từ REVIEW). **+2 bug chức năng:** `reset_for_new_project.py` không dọn `docs/domain/**` + `translation-log/doc-review-findings/decisions.md` + `ux/design-tokens.css` khi reset project mới (giờ dọn đủ). Change-log/Removed giữ nguyên (lịch sử). Selftests + smoke 35/35 + state validate xanh. |
| 2026-07-02 | proof-lock + coverage-derive | **Vá 2 lỗ "agent tuân thủ" (audit enforcement):** (1) **FM-PROOF-FORGE** — `tracking/**` miễn phase-lock nên agent Write đè được `docker-ps/health-proof/api-proof.json` (fake cả tầng bằng-chứng-harness-đo); thêm `policies.is_proof_file` + deny ở `PreToolUse(Write\|Edit)`: 3 proof file CHỈ `capture_infra_proof.py` được sinh (tracking còn lại — report/bugs/registry — vẫn ghi tự do). (2) **coverage HARNESS derive** — `coverage_pct` trong `all_boundaries_reviewed` là số agent tự khai; giờ service đã scaffold → harness đọc report thật (jacoco XML `build/reports/jacoco/**`/`target/site/jacoco` · vitest/jest `coverage/coverage-summary.json` · lcov `coverage/lcov.info`): có report = số đo THẮNG số khai; scaffold mà không report = fail; chưa scaffold = fallback khai (hermetic/smoke giữ nguyên). Mirror triết lý derive test_result. policies+gates selftest (+proof-file & coverage-derive hermetic) + dispatcher + smoke 35/35. |
| 2026-07-02 | doc-gap P0 (khớp-nối + contract) | **6 gate khớp-nối chống tài-liệu rơi im lặng giữa các khâu (audit toàn tuyến ideation→test + trục contract):** (A) `translation_parity` @domain-end — business đã KÝ ↔ eng doc 1-1 qua `source`/`domain_source_id` (translate bỏ sót = chặn; eng epics/feat/BR mồ côi không source = chặn; translator phải dịch đủ 100%). (B) `todo_resolved` @design-end — marker `TODO engineer`/`TBD (DESIGN)`/`enforcement_location: TBD` translator để lại phải được DESIGN điền hết (BR không nơi enforce = rule không bao giờ được code, chỉ lộ ở UAT). (C) `plan_integrity` +chiều ngược **FEAT mồ côi**: FEAT-*.md không vào `features[]` boundary nào = không bao giờ được build/test = chặn (opt-out frontmatter `status: deferred\|dropped`). (E) `ac_coverage` @test-plan — traceability FEAT.AC ↔ TC 2 chiều bằng máy (parse `### AC-n` vs cột feature+AC registry): AC in-scope thiếu TC = mồ côi; TC trace AC không tồn tại = stale sau /apply-cr đổi AC; token deferred bỏ qua ("coverage matrix 100%" hết là lời hứa). (K1) `contract_graph_parity` @plan — đồ thị contract (api-*.md `consumers[]` + INTEG-INT consumer/producer + events subscriber/§9) ↔ MATRIX `depends_on`/`consumed_by` khớp 2 CHIỀU: id ma / cạnh contract không có trong MATRIX / cạnh MATRIX không contract doc nào ghi nhận = chặn (3 nguồn khai 1 sự thật). (K2) `api_contract_proof` @dev-handoff — `capture_infra_proof.py` mở rộng fetch OpenAPI runtime (`/v3/api-docs` springdoc, HARNESS đo) → `api-proof.json`; endpoint khai `api-{b}.md` (bảng `Method · Path`, normalize path-param) phải tồn tại trong spec runtime = bắt contract↔implementation drift (gốc BUG-006) TRƯỚC test; GraphQL/không api doc miễn; ref-backend-config ghi rõ springdoc bắt buộc. Tất cả force-bypass+audit; gates+planning_lint+capture selftest (6 cụm mới hermetic) + smoke 35/35. |
| 2026-07-02 | anti-fake-pass + UI-fidelity | **Vá pass-ảo test + nâng UI fidelity (2 gap e2e ghi nhận: UI-test luôn né được + registry over-scope).** (1) gate `ui_test_present` @test-plan: mỗi web boundary trong wave phải có ≥1 auto-TC UI in-scope (UI toàn manual/deferred/vắng = chặn — trước đây registry toàn API-TC vẫn `test_result=pass` dù FE chưa từng render). (2) `test_evidence` @test-execute nâng 3 lớp: **screenshot bắt buộc** cho TC web boundary pass|fail (`screenshots/{TC}*.png`, check PNG/JPEG magic-bytes ≥1KB — chống UI-test khống); **skip đối chiếu health-proof** (proof nói service UP → skip "service-down" bị chặn; chết thật → re-run capture_infra_proof.py); **siết skip-marker** thành cụm-từ cụ thể (bỏ "down"/"unavailable" đơn — trước match cả "dropdown"). (3) gate `registry_scope` @test-plan: auto-TC chỉ trace FEAT thuộc wave plan ≤ wave hiện tại (registry tích luỹ — FEAT cũ hợp lệ regression; FEAT wave TƯƠNG LAI = over-scope chặn) + FEAT/AC deferred mà row thiếu tag `@deferred` = chặn (vá "TC cho feature defer → bug rác chặn end-wave"). (4) `web_styling` @dev-handoff thêm lớp 3: dùng `var(--...)` mà token KHÔNG định nghĩa/import trong bundle (không copy/`@import` design-tokens.css, không `:root{--}`) = chặn (var resolve rỗng → UI vẫn trắng dù "dùng token"). (5) `design_gate`: có web boundary → `ux/design-tokens.css` phải tồn tại. (6) Skill uplift cho "UI đẹp": ux-design +§Visual polish (app shell / spacing rhythm / type scale / component primitives / interaction states / elevation — spec cụ thể đối chiếu được); rules-web rule 44 ép copy+import design-tokens vào bundle + UI primitives dùng chung đủ hover/focus; review-web §6 +token-định-nghĩa-trong-bundle (BLOCKER) + visual states; test-execute assert computed-style theo token (trang trắng/unstyled = FAIL+bug). gates selftest (3 case mới hermetic) + smoke 35/35 + policies/dispatcher/capture selftest + state validate xanh. |
| 2026-07-10 | W1 dedup router (Lost-in-the-Middle, L04) | **Thu gọn CLAUDE.md §RUNTIME GUARANTEES — bỏ trùng lặp với PROTOCOL.md (đối chiếu khóa Harness Engineering, bài L04 'one giant instruction file'):** dòng 145 cũ = 1 bullet ~4000 ký tự nhồi toàn bộ gate của mọi command (bản sao LỖI THỜI của PROTOCOL.md §Gate evidence — thiếu `docker_compose_ok`/`connectivity_ok` mà `gates.py GATE_RULES` test-plan có → drift). Thay 12 bullet dày bằng **bảng awareness 1-dòng-mỗi-hook + pointer** tới `harness/PROTOCOL.md §Hooks/§Gate evidence/§Turn-flag`; chi tiết sống ở PROTOCOL.md (SoT). Migrate cơ chế **turn-flag #11** (chỉ tồn tại ở CLAUDE.md) sang PROTOCOL.md §Turn-flag mới (reset/tiêu-cờ/gate-fail-không-tiêu/vá Skill|SlashCommand/#12) — không mất thông tin. Kết quả router: 25.110 → 17.781 ký tự (−29%, ~1.830 tokens/session). RỦI RO LOGIC = 0 (hook đọc gates.py/STATE-MACHINE.json, không đọc CLAUDE.md). |
| 2026-07-10 | W1 tiếp — rút gọn §SLASH COMMANDS | **Viết lại mục lệnh theo dạng 'lệnh gì · tác dụng gì · (chuyển stage)' — cắt đuôi gate lặp PROTOCOL.md:** ~6 dòng dày (dev-handoff/approve-document/design-end/plan/discovery-start) nhồi chi tiết gate (coverage BE80/jacoco, doc_stamped, ADR≥3...) = bản sao §Gate evidence → cắt, thay bằng pointer 1 dòng. State transition viết CHUẨN theo `STATE-MACHINE.json` (SoT) — **sửa lỗi cũ**: `/dev-handoff` thực tế `REVIEW_DEV→DEV_HANDOFF` (text cũ ghi nhầm `→TEST_PLAN`), `/test-plan` mới là `DEV_HANDOFF→TEST_PLAN`. Router (cả W1+phần này): 25.110 → ~15.1K ký tự (−40%). |
| 2026-07-10 | W1 chốt — SLASH COMMANDS dạng bảng + move changelog | **Theo góp ý user:** (1) viết lại §SLASH COMMANDS thành **bảng `Bước | Lệnh | Tác dụng`** xếp theo happy-path 8 bước (Khám phá→Yêu cầu→Thiết kế→Kế hoạch→Rà soát→Code→Test→Đóng + Ngoài luồng CR) — tiếng Việt dễ hiểu cho người mới, bỏ prose dài dòng + mã gate. (2) `git mv docs/HARNESS-CHANGELOG.md → HARNESS-CHANGELOG.md` (root, cùng thư mục CLAUDE.md) + cập nhật pointer. Router cuối: 25.110 → 14.745 ký tự (−41%). |
| 2026-07-10 | W2' dev WIP=1 per-feat (L07) | **Dev làm TUẦN TỰ từng feat thay vì cả boundary một lần (áp L07 WIP=1 đúng chỗ — implementation, khác /design):** `build_prompt.py` `_feat_task` (start-dev) đổi 'implement MỌI AC' → 'implement TUẦN TỰ từng FEAT một (feat1→feat2→feat3): mỗi feat xong hẳn (AC+BR+test XANH)→commit→feat kế; không mở song song, không bỏ dở; làm HẾT'. Đồng bộ 2 bản sao chống drift: `_template-dev-agent.md` (§Trách nhiệm 1 + workflow step) + `commands/start-dev.md` §Agent behavior. Enforcement = advisory-in-prompt + Stop hook build/test-green (không thêm state/gate → không drift, tôn trọng triết lý derive-don't-declare). **W2 gốc (thêm status field vào FEAT) BỎ** — FEAT đã machine-readable, thêm field tay sẽ drift. smoke 36/36. |
| 2026-07-10 | W5 executable arch rules (ArchUnit, L10) | **Kiến trúc layer từ 'review-agent đọc' → 'test chạy được chặn' (vá mâu thuẫn triết lý: harness đo-không-tin mọi chỗ TRỪ layer):** (1) gate `check_code_compliance` +check (e) — backend boundary scaffold phải có ≥1 ArchUnit test (`src/test/**/*.java` import `com.tngtech.archunit`); thiếu = BLOCKER (mirror Dockerfile-missing). PASS tự động do gradle/Stop hook chạy (ArchUnit = JUnit) → không thêm gate 'chạy', chỉ ép TỒN TẠI. (2) `ref-backend-pattern §7.5` — template ArchUnit (Layered: layeredArchitecture + ctrl-no-repo + @Entity ở entities/*Entity + no-cycle; Hexagonal: domain-pure + onionArchitecture) map 1-1 với §7 Forbidden. (3) `ref-backend-config` +dependency `archunit-junit5:1.3.0`. (4) `build_prompt` scaffold task (kind==backend) + `rules-backend` §1 ép scaffold gồm ArchitectureTest. (5) `review-backend §L` hạ phụ thuộc: layer do ArchUnit chặn máy, review lo semantic/borderline. Scope: intra-boundary backend; cross-boundary FK + web/mobile giữ nguyên. gates+build_prompt syntax OK, gates selftest + smoke 36/36. |
| 2026-07-10 | Vá khe ac_coverage (FEAT 0-AC lọt verify) | **Bug thật phát hiện khi user bắt 'tự tin quá':** `check_ac_coverage` cũ dùng `if not acs: continue` — gộp 2 case (FEAT không có file / FEAT có file nhưng 0 heading `### AC-n`) → FEAT malformed (0 AC) bị BỎ QUA IM LẶNG, không bị đòi TC, lọt verify tới end-wave (planning_lint chỉ ép epic_ref/feat_type, KHÔNG ép có AC — '≥4 AC' chỉ là lời khuyên skill). Sửa: tách `acs is None` (không file → plan_integrity lo) khỏi `not acs` (file tồn tại + 0 AC → FAIL, trừ khi deferred). +selftest case (e). Đóng khe VCR mà trước đó tôi claim nhầm là 'gate ép kín'. gates selftest + smoke 36/36. |
| 2026-07-10 | journey_e2e_present (e2e đa-hop, L10) | **Vá khe e2e journey đa-hop bị hoãn tới FE (user: framework phải cover flexible):** định nghĩa e2e cũ cột vào UI (§58 'CHỈ full-stack BE+FE') → wave backend-only chỉ được `contract_test_present` phủ PAIRWISE (A→B, B→C), luồng A→B→C chạy liền không bị ép test → bug seam (data transform tích lũy/state) lọt tới khi FE tới (đắt hơn, L10). Gate mới **`journey_e2e_present`** @test-plan: phát hiện chuỗi transitive `depends_on` ≥3 boundary (dùng data MATRIX sẵn có, KHÔNG parse JOURNEY vì journey là artifact nghiệp vụ không khai boundary) → đòi ≥1 auto-TC group=e2e|integration SPAN cả chuỗi (boundary/tags đủ mọi node). **API-driven curl là đủ — KHÔNG cần FE** → backend-only wave validate luồng đa-service sớm. Chuỗi ≤2 = vacuous (pairwise=full). Mirror `contract_test_present`. Selftest hermetic (order→payment→ledger: pairwise fail / span pass / 2-boundary vacuous / force). test-plan skill §pyramid+§quy-ước + PROTOCOL gate table cập nhật. gates selftest + smoke 36/36. |
| 2026-07-11 | feat-state (L05/L07/L08 bước 1) | **Feat-state machine — bước 1: DERIVE + clock-in (nền cho WIP=1-cứng).** Đối chiếu khóa harness-engineering: điểm mù lớn nhất của kernel là scope-discipline (L07 overreach) + continuity cấp-feat (L05/L08) — harness track ở cấp boundary/wave, KHÔNG có tầng feat-state. `gates.derive_feature_states` (ghép hàm có sẵn: `_feat_file_for`+`_AC_HEADING_RE` AC của FEAT · registry TC↔AC · `_report_results` TC→pass · `_wave_deferred_tokens`) → mỗi FEAT in-scope: `passing` (mọi AC in-scope có TC pass) / `active` (một phần) / `not_started` / `deferred` / `no-file`. Là VIEW (như derive_test_result), không phải file agent khai. `capture_feature_state.py` (MAIN chạy sau /test-execute) ghi `tracking/{wave}/feature-state.md`; PreToolUse chặn agent ghi tay (FM-PROOF-FORGE +feature-state.md). SessionStart brief đọc file → dòng `features = X/Y passing · đang dở: FEAT-...` = **clock-in L05** (session mới thấy ngay feat còn dở, không dò lại). Bước 2 (gate `features_complete` @end-wave: VCR=1.0, chặn ship khi feat nửa-vời) TÁCH riêng — để feat-state tự chứng minh 1 vòng thực tế trước khi treo gate ship. gates+capture+policies selftest (hermetic) + smoke 36/36 xanh. |
| 2026-07-11 | feat-state (L07 bước 2) — WIP=1 ship-gate | **Gate `features_complete` @end-wave — WIP=1 từ lời-dặn → enforcement máy (L07).** Sau bước 1 (derive + clock-in), treo gate ship lên feat-state: `check_features_complete` derive `feature-state.md`, chặn /end-wave nếu còn in-scope feat `active` (một phần AC verified, phần khác chưa = làm dở nửa chừng = overreach). Đây là VCR=1.0 tại điểm harness quan sát được (sau test, có report) — thay cho lời-dặn WIP=1 trong prompt W2'. **Kỷ luật chống chặn-oan:** CHỈ chặn `active`, KHÔNG chặn `not_started` (AC chỉ-manual chưa ghi report = not_started; test_passed/ac_coverage/uat_signed đã phủ). Không trùng test_passed (auto-TC pass) / ac_coverage (AC↔TC design-time) — cái này bắt "feat verify được NỬA" ở run-time. Lời-dặn WIP=1 (build_prompt dev) nối tới gate ("feat làm dở = wave không đóng được") thay vì gỡ. commit-per-feat signal @DEV: DEFER (git-style đa dạng, dễ false-positive, giá trị mờ). end-wave command/PROTOCOL/CLAUDE đồng bộ. gates selftest (+3 case: passing pass / active chặn / not_started không chặn) + smoke 36/36. |
| 2026-07-11 | vá hook chặn oan sub-agent load skill (e2e RoomBook) | **Bug thật vòng test RoomBook: sub-agent invoke skill `domain-po` bị `PreToolUse(Skill\|SlashCommand)` chặn oan.** Nguyên nhân: `_pre_skill` GỘP 2 tool khác hẳn — `SlashCommand` (chạy lệnh, fire UserPromptSubmit, reset turn-flag = vector tự-nối-lệnh THẬT) và `Skill` (load convention skill, sub-agent nạp checklist của chính nó) — rồi chặn theo TÊN ∈ GATE_RULES. Vì `domain-po`/`test-plan`/… vừa là command vừa là skill-name, sub-agent load skill của nó bị deny. Fix GỐC (không phải workaround nhét-skill-vào-prompt — thứ đó phá "skill là SoT load on-demand" của L04/W1): `_pre_skill` chỉ áp GATE_RULES-check khi `tool_name == "SlashCommand"`; `Skill` tool cho qua LUÔN (không transition state → không phải vector tự-nối; để đổi state vẫn phải `harness complete` qua Bash → _pre_bash + turn-flag đã chặn). dispatcher selftest viết lại (SlashCommand harness cmd → deny; Skill harness cmd → allow) + smoke 36/36. PROTOCOL/CLAUDE hook-row đồng bộ. |
| 2026-07-11 | vá drift 'quên README' trong gate eng-doc (e2e RoomBook) | **Bug thật vòng test: `translation_parity` @domain-end fail vì `docs/architecture/feat/README.md` bị coi là eng doc MỒ CÔI** (không có `source` → tưởng tự-author né vòng ký). Nguyên nhân: 3 hàm glob `docs/architecture/{kind}/*.md` — `check_doc_stamped` NHỚ loại README, nhưng `_eng_docs_by_kind` (translation_parity) + `check_todo_resolved` QUÊN → drift "một sự thật ở nhiều nơi". Fix GỐC (không force-override — force chỉ giấu bug, wave sau lại vấp): helper chung `_is_scaffold_md(name)` (TEMPLATE/_TEMPLATE/README.md) thay 3 chỗ glob thủ công → nhất quán, chống drift lần kế. Selftest +case README-không-mồ-côi + smoke 36/36. |
| 2026-08-22 | VIPER-derived đợt 1 — Discovery khai thác sâu + /decide + /dogfood | **Ba mảng, chắt lọc từ `edu-grounding-viper` (VIPER v1.9), lấy CƠ CHẾ chứ không lấy văn xuôi.** (1) **Discovery hỏi sâu**: skill D0/D1 bỏ trần `AskUserQuestion ≤5` (budget là THỜI GIAN, không phải số câu) + tách **hai chế độ hỏi** (khám phá = hội thoại mở, CẤM AskUserQuestion vì option mớm lời; quyết định = AskUserQuestion + đánh đổi) + 4 luật đào sâu (hỏi quá khứ cụ thể · ≥1 bằng chứng · quy ra số · đào theo mạch) + playback + checklist đóng 6 mục + "dấu hiệu hời hợt". Template: `hypothesis-log` +dòng `Bằng chứng:` mỗi pain +cột Bằng chứng mỗi hypothesis +`§6 Lỗ hổng & cách xử`; `persona-pool` +`Năng lực được cấp`/`KHÔNG được làm` +**`## Ma trận vai × hành động`** (`có`/`cấm`, no-icon) +bảng gán 6 vai dogfood; `capability-map` +cột `Wave giao`/`Trạng thái` (bảng SỐNG, hết chết sau D1). (2) **`discovery_gate.py` từ shape-gate → evidence-gate**: thay `len(body)>20` bằng đếm dòng `Bằng chứng:` có nội dung thật + đòi bảng lỗ hổng + **ma trận không ô trống, có ≥1 ô `cấm`**. Port 3 primitive của họ: `read_live()` (bỏ `<!-- -->` TRƯỚC mọi phép đếm), `table_rows(drop_placeholder)`, kênh `err(msg, hint)` (mỗi ✗ mang *sửa ở đâu*). **Vá lỗ thật**: phép đếm cũ chỉ loại `_TBD_` nên 5 dòng mẫu `{{TBD}}` của `TEMPLATE.capability-map` đủ làm gate D1 XANH trên file chưa ai điền. `--selftest` 11 case **assert gate đỏ ĐÚNG LÝ DO** (không chỉ đỏ) — hồi quy cho đúng lỗ đó. (3) **`/decide`** (`scripts/decide.py`, mọi stage, không đổi stage): sổ `tracking/decisions.md` nâng 3→7 cột, dùng chung với audit force-bypass của `state.py`. Ép cơ chế chứ không dặn: **từ chối dòng mà `--why` không dẫn về artifact** (`FEAT-`/`ADR-`/tên file/`§n`) — không dẫn được về đâu = chưa đủ căn cứ tự quyết; cột `--assume` bắt buộc; `--reversible hard` phải nói khó ở đâu. `--selftest` 7 case. (4) **`/dogfood`** @MANUAL_TEST (in-state, sau `/test-execute`): 6 lăng kính persona × **2 đợt chia theo TRẠNG THÁI DB** (đợt 1 sạch — trạng thái rỗng chết ngay khi có bản ghi đầu tiên; seed lại; đợt 2 có dữ liệu), MAIN điều phối để agent không tự gộp đợt. `breaker` chạy đủ ma trận vai × hành động (mỗi ô `cấm` = 1 ca), `picky` đo computed-style/shape thật. Bằng chứng bộ ba + "dấu hiệu dogfood giả". Gate `dogfood` (`health_proof` — dogfood trên hệ chết còn tệ hơn không chạy vì để lại vết "đã kiểm") + gate **`dogfood_done` @end-wave** đọc `tracking/{wave}/dogfood-report.md` đòi đủ 6 vai + 2 đợt (đọc FILE, không đọc lời khai `evidence`). 6 agent spec đăng ký `CORE_AGENTS` (quên → reset xoá sạch, lỗi chỉ lộ ở wave sau). smoke 37/37 + gates/policies/discovery_gate/decide selftest xanh. |
| 2026-08-22 | VIPER-derived đợt 2 — vòng wave không-reset + gộp 26→10 lệnh | **(A) Vòng wave kiểu VIPER, KHÔNG RESET.** Trước đây đóng wave đi qua `done-wave` → reset STATE về BOOTSTRAP; reset = mất trí nhớ giữa các wave, wave sau không biết wave trước giao gì. Thay bằng cặp cơ chế của họ: **ĐÁNH DẤU** — `test_result_wave`/`review_results_wave` ghi kèm ở `state.apply_effects`, gate đối chiếu qua `gates.results_stale()` (fail-closed khi thiếu dấu). **Vá 2 lỗ kiểm được:** `check_test_passed` và `check_all_boundaries_reviewed` đọc field PHẲNG — chúng chỉ vô hại nhờ đúng cái reset sắp bị bỏ; không đóng dấu thì wave N+1 thừa hưởng `pass` của wave N và boundary review pass ở wave N cho `/run-wave` đi thẳng tới handoff mà chưa review dòng nào (docstring `all_boundaries_reviewed` còn tự khai "wave-scoped" trong khi list chỉ khoá theo boundary — đã sửa). **SNAPSHOT** — `scripts/next_wave.py`: copy **toàn bộ** `docs/` + `knowledge-base/` + `tracking/wave-N/` + STATE/MATRIX/decisions → `archive/wave-N/`; chép cả ĐẶC TẢ chứ không chỉ thực thi (wave sau lùi `/domain` sửa FEAT thì bản wave cũ vẫn nguyên để đối chiếu — chép mỗi tracking/ thì `DELIVERED.md` thành cái nhãn). Chép hết không chọn lọc: "chép doc nào" là phán đoán sẽ mục, ~0.5MB/wave nên chọn lọc không đổi lại gì. Sự TỒN TẠI của `archive/wave-N/` = **cờ "đã đóng"** (chống đóng hai lần), thay `teardown_ok` agent tự khai. **`DELIVERED.md`** — đóng gói FEAT + AC đã verify + TC nào verify, máy derive từ `derive_feature_states` (registry+report), tách "đã giao" (hợp đồng) khỏi "hoãn" (không phải hợp đồng); là hợp đồng regression wave sau phải giữ. **Nối 3 nơi đọc:** prompt dev (`_prior_waves` đọc từ ĐĨA không suy theo số wave) nhận khối "TÔN TRỌNG WAVE TRƯỚC — chỉ THÊM vào surface đã giao; buộc phải phá → DỪNG ghi blocker, KHÔNG tự quyết" · `dogfood-rushed-agent` lượt regression · skill `dogfood`. `next_wave --selftest` 12 assert, khoá là 3 cái *gate ĐỎ LẠI sau khi mở wave* (bài học từ `test_repeat` của VIPER — không có assert đó thì cả cơ chế chỉ là lời hứa). **(B) Gộp 26 → 10 lệnh.** `/discover`(2) · `/domain`(5, bỏ mode — tự suy thiếu gì viết nấy, user OK = chữ ký) · `/design`(3, **bỏ cờ `--ux`** — chạy UX suy từ kind boundary, backend-only thì bỏ qua *và nói rõ là bỏ qua*) · `/run-wave`(7 chốt: start-wave→start-dev→review-dev→dev-handoff→test-plan→test-execute→dogfood, +fix bug) · `/next-wave`(2) · `/status`(mới). **State machine + GATE_RULES giữ NGUYÊN** — chỉ đổi lớp vỏ, mỗi slash command gọi nhiều `harness <id> complete` như cũ, gate từng chốt chạy đủ. **Gỡ turn-flag** (#11): hành lang phải chạy nhiều complete trong một lượt; thứ nó thật sự bảo vệ ("đi tiếp khi chưa đủ điều kiện") là GATE, vẫn chạy từng chốt — turn-flag chỉ ép gõ lệnh giữa các chốt ĐÃ XANH, đó là ma sát. 4 lệnh đổi dạng thành cơ chế: `decide`→NON-NEGOTIABLES prompt · `log-bug`→skill `bug-logging` · `fix-bugs`→chốt trong `/run-wave` · `apply-cr`→thay đổi = wave sau. +transition `next-wave: DONE→WAVE_OPEN`. smoke 37/37 + gates/policies/dispatcher/discovery_gate/decide/next_wave selftest + state validate xanh. |
| 2026-08-22 | VIPER-derived đợt 3 — sổ tương thích ngược + challenge trước khi code | **Hai luật của VIPER mình chưa lấy, giờ lấy — và lấy dạng CƠ CHẾ chứ không lời dặn.** (1) **`tracking/BC-LEDGER.md`** (VIPER luật #1/#4 — legacy là hợp đồng): §1 sổ hợp đồng surface đã giao (API/DB/cache/event/webhook/tích-hợp, cột `Wave giao`) — **tích luỹ VĨNH VIỄN, không wave nào xoá**; §2 luật đổi additive-first per loại (bảng tham chiếu — chỗ khối "chỉ THÊM" trong prompt dev đối chiếu vào); §3 checklist rà mỗi wave; §4 bỏ qua có chủ đích. Gate **`backward_compat`** @end-wave wave ≥2 đếm §3; `next_wave.rearm_bc_ledger()` bỏ tick **ĐÚNG §3** khi mở wave (giới hạn phạm vi có chủ ý — VIPER từng vấp: bỏ tick cả file thì một checkbox ghi chú ở §1/§4 chặn đóng wave vĩnh viễn mà không chỗ nào re-arm; selftest của họ có hẳn assert cho việc này, mình assert lại). Vì sao cần dù đã có dogfood regression: **dogfood đi lại LUỒNG, gate này soi HÌNH DẠNG** — luồng vẫn chạy trơn trong khi field response bị đổi tên, client ngoài gãy mà dogfood không thấy vì nó dùng đúng client mới. Đây cũng là hàng rào thay cho `/apply-cr` vừa bỏ ở tầng contract (`DELIVERED.md` phủ tầng FEAT/AC). (2) **`tracking/challenge-log.md`** (VIPER luật #8a — đối kháng nội bộ thay reviewer ngoài): trước dòng code đầu tiên, dev tự ra MỘT câu hỏi khó dựa trên spec THẬT (mâu thuẫn giữa hai AC · ca biên HLD chưa chặn · **ô `cấm` trong ma trận vai × hành động** · thứ trong FEAT nhưng ngoài scope wave · surface `BC-LEDGER §1` sắp đụng), trả lời CHỈ từ spec, tự chấm PASS/FAIL — **FAIL = chưa được code**. Gate **`challenge_passed`** @review-dev, **lọc theo wave** (PASS wave trước không gánh hộ wave sau — cùng cơ chế đếm-theo-wave với test_result/review_results) và **FAIL không phải gate đã qua**. Vì sao: review-dev bắt lỗi SAU khi code xong, lúc đó cái giá đã trả; challenge bắt đúng chỗ "tưởng đã hiểu mà chưa". Nhắc trong prompt dev (`build_prompt` task đầu tiên của start-dev) + `tracking/BC-LEDGER.md` thêm vào danh sách doc dev đọc khi wave ≥2. 10 phép kiểm hermetic cho 2 gate (gồm: tick ngoài §3 không cứu được §3 · PASS wave-001 không gánh wave-002 · chỉ-FAIL vẫn đỏ) + next_wave selftest 15 assert + smoke 37/37 + gates/policies/dispatcher/discovery_gate/decide selftest + state validate xanh. |
| 2026-08-22 | vá hook false-positive + reanchor sau compact | **(1) `parse_harness_complete` chặn oan lệnh ghi tài liệu.** Regex quét CẢ chuỗi command nên `cat > commands/run-wave.md <<'EOF' … py scripts/harness.py start-wave complete … EOF` bị coi là ĐANG CHẠY stage-command, rồi deny vì gate lệnh đó không đạt — chặn đúng lúc đang viết chính tài liệu mô tả lệnh (vấp 3 lần khi gộp command, phải né bằng Write tool). Fix: `strip_heredocs()` bỏ thân heredoc TRƯỚC khi phân tích — **hook phải xét lệnh LÀM GÌ, không phải nó CHỨA CHỮ GÌ**; cùng họ với luật `guard_ds` của VIPER ("chỉ chặn lỗi MỚI, không bắt đền nội dung vốn có — hook báo oan là hook sẽ bị tắt"). Selftest +4 case: doc-có-ví-dụ-lệnh không bị nhận · heredoc không trích dẫn cũng vậy · lệnh THẬT vẫn nhận đủ evidence · lệnh thật đứng SAU heredoc vẫn bị bắt. **(2) Reanchor sau compact** (port `reanchor.py` của VIPER): `SessionStart` matcher `startup|resume` → `startup|resume|compact`; `source == "compact"` → `policies.reanchor_after_compact()` nhồi lại **NON-NEGOTIABLES đọc THẲNG từ CLAUDE.md** + trạng thái sống, thay vì chỉ brief stage/wave như trước. Không chép cứng luật vào hook (bài học của họ: chép cứng = bản sao thứ hai, sửa CLAUDE.md mà quên hook thì nhồi luật CŨ vào đúng lúc MAIN mất trí nhớ — hỏng hơn không nhồi). Vì sao gấp: PreCompact của mình chỉ ghim TRẠNG THÁI, không ghim luật; và từ khi **gỡ turn-flag**, kỷ luật hành lang ("chốt đỏ → DỪNG, không force, không nhảy chốt") sống hoàn toàn bằng văn xuôi — đó chính là thứ compact làm phẳng đầu tiên. Nhánh `startup` giữ nguyên brief ngắn (không nhồi luật mỗi lần mở phiên). smoke 37/37 + toàn bộ selftest xanh. |
| 2026-08-22 | permission 3 lớp (deny→ask→allow) | **Trước: `deny` 10 rule TOÀN BỘ là `Read(**/target/**)`/`node_modules`/`*.pyc` — chống phí context, KHÔNG rule an toàn nào; `ask` = 0; `allow` = 0; không `defaultMode`.** Tức hai lớp quan trọng nhất đang trống, không phải "triết lý khác" mà là chưa có. Lấy mô hình 3 lớp của VIPER, dịch sang bối cảnh design-repo. **`deny` +13 an toàn**: secret (`.env*`, `~/.ssh`, `~/.aws` — hook không nhìn thấy nội dung file nên permission là lớp DUY NHẤT chặn được) · `sudo` · `rm -rf /` · `rm -rf ~` · `git push --force`/`-f` · `git reset --hard origin`. Giữ nguyên 10 rule chống-ồn. **`ask` 9 rule** — hướng RA NGOÀI / không thu hồi được, dịch sang mình (bỏ deploy PaaS vì design-repo không deploy): `git push` · `gh repo create` (polyrepo — /run-wave scaffold boundary thành repo riêng) · `gh release` · `gh pr create` · `npm publish` · `docker compose down --volumes`/`-v` · `docker volume rm` · `docker system prune`. **`allow`**: `Edit` `Write` `Bash` trần + WebFetch localhost, `defaultMode: acceptEdits` — bằng chứng đo được của họ: "v1.0 liệt kê ~50 prefix lệnh vẫn dính >1000 prompt/dự án". **Thứ tự land là ràng buộc, không phải sở thích**: `allow` rộng chỉ an toàn KHI `deny`+`ask` đã đầy; bê `allow: [Bash]` sang lúc `ask` còn rỗng thì tệ hơn hiện trạng. Khác VIPER ở một điểm nghiêng về phía mình: harness có 9 hook làm phần nặng (phase-lock/protected-file/owned_paths/gate) nên permission là hàng rào NGOÀI, tập trung đúng thứ hook không thấy; họ chỉ 3 hook nên lớp permission gánh nhiều hơn. Verify: ghi `harness/STATE.json` (protected) → deny · ghi `hld-x.md` ở BOOTSTRAP (phase-lock) → deny, **dù `Bash`/`Write` đã allow trần** — hai lớp độc lập. selftest 8/8 + smoke 37/37. |
| 2026-08-22 | vá rác test lọt commit + reset không biết archive/ | **Bug thật, do chính tôi gây ra và đã push.** Khi kiểm `_prior_waves()` tôi tạo fixture `archive/wave-001/DELIVERED.md` bằng script tạm; lần chạy đầu crash `NameError` TRƯỚC dòng dọn dẹp, lần hai thấy thư mục đã tồn tại nên `made=False` → bỏ qua dọn luôn; `git add -A` cuốn vào commit `140d975`. **Không vô hại:** sự TỒN TẠI của `archive/wave-N/` chính là cờ "wave N đã đóng", nên project mới fork từ template sẽ KHÔNG BAO GIỜ đóng được wave 1 (`next_wave.checks` từ chối). Xoá `archive/`. **Lỗ cùng loại, có cấu trúc:** `reset_for_new_project.collect_targets()` dọn `tracking/wave-*/` nhưng không biết `archive/` (ra đời sau) — thêm `archive/wave-*/` + hai sổ mới `tracking/{BC-LEDGER,challenge-log}.md` vào danh sách xoá + docstring. Verify bằng monkeypatch `repo_root` trên tmpdir, KHÔNG ghi vào repo — đúng thứ đã làm sai. Bài học ghi tại chỗ vá: artifact mà **sự tồn tại mang ngữ nghĩa** thì fixture test tuyệt đối không được sinh trong repo thật. selftest 8/8 + smoke 37/37. |
| 2026-08-22 | Discovery: rà chéo + user ĐỌC và ký ở chốt D3 | **Lỗ user chỉ ra, kiểm được: discovery là lớp DUY NHẤT không có chữ ký.** domain ký bằng `domain_approve.py`, design ký bằng `approve_document.py`, còn discovery thì template có sẵn field `status:` mà KHÔNG script nào stamp — xác nhận của user nằm trong chat, không thành hiện vật. Đúng thứ harness bắt mọi chỗ khác phải có ("lời khai không phải bằng chứng"), lại thiếu ở lớp THƯỢNG NGUỒN nhất. **Lỗ thứ hai chồng lên: đọc quá muộn.** `/review-document` + `/approve-document` đều `when_state: [REVIEW]`, tức bản sanity-check *có* quét discovery nhưng chạy SAU cả domain+design+plan — tìm ra lỗ ở `hypothesis-log` lúc đó là phải tháo ngược ba stage. Cùng cái sai mà challenge đã vá ở tầng code (bắt trước khi code, không dựa vào review sau khi code) nhưng tầng tài liệu thì để nguyên. **Vá:** `approve_document.py` chuyển từ duyệt-thư-mục sang **glob theo LỚP** (`STAMP_PLANS["discovery"|"design"]`, CLI `--layer`) — glob vì discovery có file LỒNG (`boundaries/<b>/CHARTER.md`) và file LẺ ngoài thư mục của nó (`docs/architecture/PROJECT.md`, discovery sở hữu theo phase-lock). Gate mới **`discovery_stamped`** @`discovery-end` (mirror `domain_stamped`): chặn complete chay, vacuous pass khi chưa author gì. `/discover` + skill `boundary-charter` thêm **Chốt D3 ba bước**: (1) agent rà chéo toàn lớp — hypothesis↔capability↔persona↔ma trận↔ES↔boundary↔PROJECT, lệch thì SỬA TRƯỚC không đẩy sang user phát hiện hộ; (2) **DỪNG LẠI**, trình danh sách file kèm *mỗi file nên soi gì* + chỗ đã tự quyết (trỏ decisions.md) + **chỗ mình không chắc nhất**, KHÔNG tự ký KHÔNG chạy tiếp; (3) user góp ý → sửa → rà lại → trình lại (lặp không giới hạn) · user **duyệt = chữ ký** → stamp → complete. Cùng khuôn với `/domain`. selftest 9/9 + smoke 37/37. |
| 2026-08-22 | ca biên thành bảng TRA + ranh giới liên boundary (VIPER mục 13) | **Đo template trước, không đoán:** `TEMPLATE.hld` CÓ "dịch vụ ngoài hỏng thì sao" + "thứ cố tình không làm", `data-model` CÓ ràng buộc per bảng, `feat` CÓ AC BDD + ngoài phạm vi, `business-rule` CÓ `Tình huống → Xử lý`. **THIẾU, lặp ở ba template: không chỗ nào khai CA BIÊN kỹ thuật/dữ liệu** — ca biên nghiệp vụ có nhà (`BR-*`) nhưng "gửi hai lần trong 200ms · sửa đồng thời · xoá mềm hay cứng · gọi sai thứ tự · hỏng nửa chừng · đọc bản cũ" thì không, nên agent lúc code buộc phải đoán và mỗi boundary đoán một kiểu. Đây đúng mục 13 của VIPER: *rule trong văn xuôi phải suy diễn → có khoảng trống hiểu sai; rule trong bảng `Tình huống → Xử lý` thì tra bảng → không có khoảng trống*. **Vá:** `HLD §6.1 Ca biên đã quyết` — **checklist ĐÓNG 8 dòng** (E1-E8: gửi hai lần · sửa đồng thời · xoá · sai thứ tự · hỏng nửa chừng · bản cũ · rỗng · thu hồi quyền giữa chừng) + bảng riêng cho ca biên đặc thù boundary (E9+); cột **`Enforce ở đâu` phải trỏ thứ CHẶN ĐƯỢC** (unique index · `version` + `WHERE version=?` · idempotency key · state machine) — "validate ở service" là chưa quyết gì. `HLD §6.2 Ranh giới liên boundary` — ĐƯỢC gọi qua đường nào / **KHÔNG được** / vì sao (ArchUnit chặn luật chung, nhưng "RETRIEVAL không gọi KM qua REST vì đã có DB grant" là quyết định riêng của hệ). `FEAT §6.1 Ca biên hành vi` — 6 dòng (B1-B6) ở tầng người dùng thấy gì, mỗi dòng cần chặn ở server phải sinh ≥1 AC âm; `/dogfood` chạy đúng các tình huống này. Gate mới **`edge_cases_decided`** @`design-end`: quét mọi `hld-*.md`, ô `Xử lý` trống hoặc còn `{{…}}` → đỏ kèm ĐÚNG mã dòng. **`n/a — <lý do>` TÍNH LÀ đã trả lời** — checklist đóng ép RÀ QUA hết, không ép LÀM hết (mirror BC-LEDGER §3 + ma trận quyền). 6 phép kiểm hermetic (vacuous · thiếu §6.1 · ô trống nêu đúng mã · n/a xanh · TEMPLATE không bị tính · force). skill `technical-design` +mục dạy điền. selftest 9/9 + smoke 37/37. |
| 2026-08-22 | gộp nửa sau vào /domain (10→7 lệnh) + gate regression + dọn rác | **(A) `/domain` thành hành lang nửa sau — 9 chốt**, nuốt `/design` `/plan` `/review-document`: nghiệp vụ (chốt DUY NHẤT được hỏi) → ký → dịch → ADR/HLD/API/data-model/events/tích hợp → UX (chỉ khi kind web/mobile, backend-only thì bỏ qua *và nói rõ*) → chia wave → **rà chéo toàn bộ** → dừng REVIEW. `/approve-document` giữ nguyên làm chỗ **kết thúc phần tài liệu = khoá scope** (user chốt giữ). Từ chốt 3 trở đi gặp mơ hồ → `decide.py` rồi đi tiếp, KHÔNG dừng hỏi — thứ cần hỏi lẽ ra đã hỏi ở `/discover` và chốt 1. State/gate/phase-lock giữ nguyên, chỉ đổi vỏ. **10 → 7 lệnh**: `/discover` `/domain` `/approve-document` `/run-wave` `/dogfood` `/next-wave` `/status`. **(B) Gate `regression_tc_present`** @test-plan wave ≥2 — **lỗ user chỉ ra khi hỏi "đã nhắc wave tôn trọng wave trước chưa"**: đo thì thấy `start-dev`/`fix-bugs`/`dogfood` CÓ nhắc thật, còn `review-dev`/`dev-handoff` chỉ TRÙNG CHỮ, và `test-plan` mới chỉ *cho phép* TC wave trước chứ không *đòi*. Gốc: registry nằm ở `tracking/{wave}/` = **file MỚI mỗi wave**, không gì mang TC cũ sang → wave 2 sinh registry sạch là `test-execute` chỉ chạy TC wave 2, **không gì chạy lại test wave 1**, regression chỉ còn trông vào dogfood. Câu "registry tích luỹ" trong mô tả gate cũ là giả định chưa ai cưỡng chế. Gate đọc `archive/wave-*/DELIVERED.md` lấy FEAT **`passing`** của các wave trước (FEAT hoãn/dở KHÔNG phải hợp đồng, không ép) → registry wave này phải có TC trace tới từng cái. Đây là bất biến VIPER phát biểu thẳng: *smoke test vòng cũ đi theo `make test` và phải GIỮ XANH* — với mình `make test` chính là registry. 6 phép kiểm hermetic. **(C) Dọn rác:** `check_regression_tc_present` từng viết xong mà **chưa nối dispatch/GATE_RULES** — gate chết trông như gate sống, loại rác tệ nhất; đã nối. Xoá 3 command file bị nuốt + 36 tham chiếu chết trong skill + 16 trong CLAUDE.md + description bị thay trùng (`/domain … /domain`) + bảng SLASH COMMANDS mất dòng `/approve-document` và nhảy số bước. Thêm phép kiểm toàn vẹn (gate chết · kind khai mà không dispatch · nhánh dispatch thừa · tham chiếu lệnh đã xoá · chuỗi lặp máy móc). selftest 9/9 + smoke 37/37. |
| 2026-08-22 | lấy NỘI DUNG của VIPER (design system · chốt mockup · sẵn-sàng-vận-hành) + `read_live()` + quét tài liệu trôi | **Ba lỗ đầu cùng một hình dạng: harness ép tuân thủ những thứ chưa được định nghĩa.** (1) **`DESIGN-SYSTEM.md`** — có `design-tokens.css` (giá trị token) nhưng thiếu phần máy KHÔNG suy được: ba tính từ + **neo tham chiếu THẬT** user chỉ ra · cặp tương phản · **kho component ĐÓNG** (mỗi khối: dùng ở màn nào + **trạng thái bắt buộc**) · ba khuôn rỗng/lỗi/đang tải. §4 đắt nhất khi bỏ: vai `picky` được giao đi kiểm "component thiếu trạng thái bắt buộc" mà không chỗ nào khai trạng thái bắt buộc là gì. Gate `design_system_closed` @`design-end` **tự tính WCAG từ hex** thay vì tin lời khai (bắt được `#9ca3af` trên trắng = 2.54). Skill `ux-design` +luật thứ tự BẮT BUỘC: design system TRƯỚC, mockup SAU. (2) **`mockup_signed`** @`approve-document` — `design-tokens` + `web_styling` + vai `picky` đang bảo vệ một bản thiết kế **chưa ai xem và duyệt**; đòi dòng `Chốt bởi user: <ISO>` ở SCREEN-MAP, backend-only thì bỏ qua. Phản hồi hình thức → sửa TOKEN để lan ra mọi màn, KHÔNG sửa tay từng mockup. (3) **`PRODUCTION-READY.md`** — đo được **bảy chỗ không gate nào chạm**: backup · rate limit · error tracking · structured log · secret · HTTPS · rollback. Bản đầu tôi tự chế `n/a — <lý do>` tự do; **giải nén VIPER đối chiếu thì sai ba chỗ**, đã sửa theo đúng khuôn họ: mỗi mục có dòng **`Kiểm:`** chạy được (grep secret trong `git log -p`, curl `/health/ready`, đăng nhập A gọi tài nguyên B phải 403 — *chặn ở UI không tính*) · **Rollback là khối ĐIỀN** `_CHƯA ĐIỀN_` (lệnh · mất bao lâu · dữ liệu · ai bấm) chứ không phải ô tick, vì lúc hỏng không ai còn bình tĩnh đọc tài liệu dài · trả lại **nhóm 4 Đo phản ứng** (analytics · event · kênh feedback · **ngưỡng con số** go/pivot/kill) — nhóm này là đầu kia của `hypothesis-log`, bỏ nó là tự cắt sợi chỉ đang đứt. Bỏ `n/a` tự do, thay bằng **từ vựng cố định** `(sau môi trường thật)` (gate KHÔNG đếm — harness dừng ở `/next-wave`, ép thì thành gate chết bị force-bypass mãi mãi) + `(mỗi wave)` (`rearm_marked()` bỏ tick khi mở wave, hạ tầng giữ tick) + bảng "Đã cố tình bỏ qua" có cột **Làm lại khi**. Gate đếm **theo từng nhóm** — "còn 18 mục" không nói được đang hổng ở bảo mật hay ở đo đạc. (4) **`read_live()`/`live()` dùng chung** — template để dòng MẪU trong `<!-- -->` cho người điền dễ bắt chước; 52 chỗ đọc tài liệu trong `gates.py` mà chỉ **1 chỗ** strip comment, tức các gate còn lại **đang đếm cả ví dụ** và xanh khi chưa ai viết gì. Strip ở parser dùng chung (`_parse_md_table_rows` phủ registry/report/screen-map, `_bc_section3`, ca biên) + 6 chốt đếm trực tiếp. Comment chiếm TRỌN dòng phải bay cả dòng: parser bảng reset header ở dòng non-pipe nên một dòng trắng chen giữa header và hàng đầu làm mất cả bảng. (5) **`doc_integrity.py` +4 lớp soi** — quét thêm `README`/`SETUP-GUIDE`/`AGENTS`/`tracking·kb·architecture README`; bắt **con số "N lệnh" khai sai** (CLAUDE.md còn ghi "10 lệnh", README "24 commands"/"19 slash commands", AGENTS "24") và **link markdown chết** (`agents/apply-cr-agent.md` sau khi agent bị xoá); in **theo nhóm file** thay vì cắt phẳng ở 40 dòng (phần bị cắt đọc như "hết rồi"). Chạy ra **114 chỗ trôi** → viết lại `README.md` + `SETUP-GUIDE.md` + `AGENTS.md` + `tracking/README.md` cho đúng mặt 7 lệnh, gỡ nốt tàn dư `apply-cr` (skill BA, agents/README, architecture/README, PROTOCOL, build_prompt, `TEMPLATE.cr.md`), **xoá bảng gate chép tay** trong SETUP-GUIDE (thay bằng `gates.py --list`). Về 0 chỗ trôi. selftest 10/10. |
| 2026-08-22 | gộp chốt UX còn MỘT · siết chốt implement bám sát FEAT/AC (học `viper-implement`) | **(1) Chốt UX gộp còn một.** Trước đó mình dừng HAI lần cho cùng một việc: `/domain` chốt 6 (mời user mở mockup bấm thử) rồi `/approve-document` (gate `mockup_signed`). Đối chiếu VIPER thì họ **có** chốt đó — `VIPER.md:263` "*dừng lại cho Authority bấm thử… tới khi Authority chốt — rồi mới được khoá scope*", `:150` "*ba chốt cuối (Authority chốt prototype nếu có UI · challenge pha V · khoá scope) giữ nguyên ở cả hai đường*" — nhưng đặt nó **ngay trước khoá scope**, và sau đó `guard_ask.py` chặn cứng `AskUserQuestion` nên I→P→E→R chạy một mạch không hỏi lại. Họ không bỏ chốt, họ **gom việc hỏi vào một pha rồi khoá**. Nên: `/domain` chốt 6 vẽ xong **đi tiếp**, chỉ để lại thứ đáng xem (đường dẫn mockup + màn nên xem trước + đủ ba khuôn rỗng/lỗi/đang tải, để trống dòng `Chốt bởi user:`); `/approve-document` thêm **bước 2b** mời xem giao diện — nơi user vốn đang đọc cả bộ tài liệu — rồi mới ký. Luật "phản hồi hình thức → sửa TOKEN, KHÔNG sửa tay từng mockup" chuyển sang đó. **(2) Chốt implement bám AC.** So `viper-implement` với `start-dev`: phần **đánh giá** mình mạnh hơn họ (`ac_coverage` TC↔AC, `derive_feature_states` per FEAT — họ chỉ tick AC trong ROADMAP), phần **làm** hụt năm thứ, vá cả năm. **(a) Vòng per-AC**: cũ là "implement MỌI AC của feat rồi test" — gộp cả feat mới thử thì lúc đỏ không biết AC nào sai; mới là `implement AC → **TỰ BẤM THỬ Ở LOCAL** (gọi API / bấm màn thật, KHÔNG phải đọc lại code) → commit`, hết AC mới enforce BR + test feat. Vá cả nhánh MATRIX-có-features lẫn nhánh fallback (hai nhánh lệch nhau là lỗ tự đẻ). **(b) Đường đi xuyên suốt TRƯỚC**: service lên → health 200 (phân biệt *sống* / *sẵn sàng*) → một endpoint (hoặc màn rỗng) → một lượt ghi-rồi-đọc-lại → commit; **chưa thông thì KHÔNG làm gì khác**. Lỗ cũ: `health_proof` chỉ soi việc này ở `dev-handoff`, tức SAU khi đã viết hết code — cùng hình dạng với lỗi design-system-sau-mockup. **(c) "BÁM VÀO ĐÂU"** — 6 ràng buộc cho backend, 9 cho web/mobile/bff: mỗi tài liệu ràng buộc MỘT thứ chứ không phải đọc tham khảo (AC = spec, KHÔNG hạ AC cho dễ làm · BR enforce **ở server** · **mỗi ô `cấm`** trong ma trận vai × hành động chặn ở server, **chặn ở UI KHÔNG tính** · ranh giới tầng: logic sai tầng là **lỗi**, không phải chuyện phong cách · ca biên §6.1 xử **ngay khi làm phần liên quan** · BC-LEDGER §1 wave ≥2; thêm cho UI: **việc ĐẦU TIÊN khi đụng UI** là đổ token vào theme — *token là thứ duy nhất được chép nguyên từ thiết kế sang code* · kho component §4 đủ trạng thái bắt buộc · mockup user đã chốt). Cũ chỉ liệt kê "DOCS TO READ" mà không nói mỗi cái ràng buộc gì. **(d) Danh sách KHÔNG làm ở chốt này**: tối ưu hiệu năng · UI đẹp hơn mức đủ dùng · test cho hàm tiện ích · tính năng "tiện tay" ngoài AC · refactor ngoài phạm vi feat — mỗi thứ chèn thêm là một thứ review và test phải soi mà không AC nào đòi. **(e)** Gỡ 59 chỗ `/​<chốt>` trong `build_prompt.py` (sub-agent đọc `# SPAWN PROMPT — /dev-handoff` thì tin là có lệnh đó để gọi) + sửa `NON_NEGOTIABLES` #5 còn dặn `/apply-cr` + `/review-document` đã chết; `doc_integrity` +`scripts/build_prompt.py` vào tầm quét vì **chuỗi trong đó là tài liệu AGENT đọc**. Gỡ 2 dòng turn-flag chết ở CLAUDE.md (hook đã gỡ từ trước, router vẫn mô tả như đang sống → agent tự dừng giữa hành lang). selftest 10/10. |
| 2026-08-22 | dogfood: bảng XỬ (từ vựng đóng) + mang phát hiện treo sang wave sau | **Gỡ sổ bug thì phải giữ QUYẾT ĐỊNH XỬ** — chỗ này tôi nói hụt và user chỉ ra. VIPER không có `bugs.md`/`BUG-NNN`/vòng đời status, nhưng `/viper-dogfood` Bước 4 ép phân loại rất chặt vào **ba đích**, và `repeat.py` mang thứ còn treo sang vòng sau. Mình thiếu cả hai: `dogfood-report.md` được gate `dogfood_done` đọc mà **không có template nào**, còn skill thì dặn ghi phát hiện vào `bugs.md` — tức là bản sao thứ ba của cùng một sự thật (kết quả đã nằm ở `test-report.md`). **(1) `TEMPLATE.dogfood-report.md`** — §1 lượt chạy (6 lăng kính × 2 đợt) · §2 bảng phát hiện với cột **`Xử` từ vựng ĐÓNG**: `sửa ngay` (hỏng luồng lõi · mất dữ liệu · **thủng phân quyền** · **gãy luồng wave TRƯỚC** `archive/wave-*/DELIVERED.md` · lệch design-system/mockup đã chốt · nhỏ dưới 15 phút) / `chưa xử` (cần thời gian nhưng TRONG scope) / `wave sau` (ngoài scope đã khoá). **Ô trống = chưa ai quyết**, không phải "không đáng" — cùng khuôn checklist đóng với ma trận quyền · ca biên · BC §3 · production-ready. §3 Kết luận điền bằng **SỐ** không phải tính từ (`AC: x/y LÀM ĐƯỢC THẬT — bấm ra kết quả, không phải đọc code suy ra` · `Phân quyền: x/y ô cấm đã thử` · `Wave trước: x/y luồng còn chạy` · `Design system: x màu ngoài token · y/z component đủ trạng thái`). §4 **Dấu hiệu dogfood giả**: cả 6 lăng kính báo "không thấy gì" ngay lượt đầu thì gần như chắc chắn chúng không thực sự dùng — hai cột *thao tác đã làm* / *thấy gì trên màn hình* ở §2 chính là chỗ phân biệt dùng thật với đọc code. **(2) Gate `dogfood_done` +3 phép**: thiếu ô `Xử` → đỏ · khai `sửa ngay` mà cột `Ở đâu` trống → đỏ (sửa rồi thì dẫn được ra commit/TC) · đẩy `wave sau` mà không dẫn `WAVE-SEQUENCE.md` → đỏ (*"đẩy sang wave sau mà không có chỗ nhận thì là bỏ đi, gọi tên khác thôi"*). Phép cuối trả lời luôn câu treo từ phiên trước — **phát hiện ngoài scope rơi vào đâu**: một dòng trong WAVE-SEQUENCE, không cần artifact backlog riêng. Bỏ dòng rỗng `D2` khỏi template (template không được ship dòng làm gate đỏ) + skip dòng rỗng trơn để khỏi báo oan. **(3) `carry_open_findings()` @`/next-wave`** — lỗ thật: `dogfood-report.md` nằm trong `tracking/wave-N/`, đóng wave là vào `archive/` rồi **không ai đọc nữa**, thứ ghi "chưa xử, sẽ làm" biến mất đúng lúc đáng ra phải nhắc. VIPER giữ được vì `repeat.py` KHÔNG reset `STATE.md` nên bảng `## Phát hiện từ dogfood chưa xử` tự đi theo, và mở vòng thì đếm + **cảnh báo, không chặn** (*"bỏ dở trong im lặng thì vòng sau không biết mình đứng trên nền gì"*). Port: chép dòng `chưa xử` sang `tracking/wave-{N+1}/dogfood-report.md` mục `## Mang sang từ wave N`, **ô `Xử` BỎ TRỐNG** — gate wave mới đỏ tới khi quyết LẠI; giữ nguyên chữ "chưa xử" thì nó tự hoãn vô hạn, mà hai wave liền mang cùng một dòng nghĩa là nó không phải "chưa xử" mà là "wave sau" hoặc "không làm". APPEND chứ không ghi đè (skill dogfood được dặn file có thể đã tồn tại). 6 phép kiểm riêng. selftest 10/10. |
| 2026-08-22 | sửa luật KHÔNG TUÂN THỦ NỔI: tách chỗ GHI khỏi chỗ NHẬN của phát hiện `wave sau` | User hỏi "họ ghi vào ROADMAP còn mình ghi chỗ này à" — soi ra gate vừa land **ra một luật máy tự chặn**. Gate `dogfood_done` đòi dòng `wave sau` phải dẫn tới `docs/plans/WAVE-SEQUENCE.md`, nhưng `phase_lock_violation('docs/plans/WAVE-SEQUENCE.md', 'MANUAL_TEST')` = **CHẶN** — mà MANUAL_TEST đúng là chỗ `/dogfood` chạy. Agent chỉ ghi được cái TÊN file vào ô, không thêm được dòng nào ở đó: đúng cái "đẩy sang wave sau mà không có chỗ nhận" mà gate định chống. Khác biệt gốc với VIPER: `ROADMAP.md` của họ ghi được bất cứ lúc nào (không có phase-lock), mình có, nên phải **tách đôi**: **GHI** ở `tracking/wave-N/dogfood-report.md` (không khoá — ghi được ngay lúc thấy) · **NHẬN** ở `docs/plans/WAVE-SEQUENCE.md` (khoá chỉ mở ở PLAN/REVIEW). Gate `dogfood_done` đổi sang đòi cột `Ở đâu` nêu **vì sao ngoài scope + cần gì để làm** (dòng cụt thì wave sau không dựng lại nổi bối cảnh), bỏ đòi hỏi dẫn WAVE-SEQUENCE. `unplanned_deferrals()` @`/next-wave` đối chiếu mã dòng `wave sau` với WAVE-SEQUENCE, **cảnh báo chứ không chặn** — cùng lựa chọn với `repeat.py` của VIPER: từ chối một phát hiện vẫn là quyền của người, nhưng từ chối trong im lặng thì wave sau không biết mình đứng trên nền gì; muốn nhận thật thì lùi `/domain` chốt chia-wave. Gỡ ref chết `docs/plans/BACKLOG.md` ở `next_wave.py` (file đã xoá từ phiên trước). 5 phép kiểm. selftest 10/10. |
| 2026-08-22 | đối chiếu TOÀN BỘ cơ chế VIPER một lượt (thay vì vá từng lỗ) — port 4 cơ chế | User: *"nên lôi hết của họ sang, nói cái gì hổng cái đó"* — đúng, vá từng lỗ là sai phương pháp. Đọc trọn `VIPER.md` (54KB) + 10 lệnh + `gate.py` + 3 guard + `repeat.py`, lập bản đồ **mọi** cơ chế rồi soi một lượt. Kết quả: 8 chỗ thiếu thật (và 1 dương tính giả — grep tưởng có mốc wave trong `decisions.md`, thực ra không). Port 4 cái rẻ + đắt giá nhất trong đợt này. **(1) `challenge_doc` @`/approve-document`** — VIPER challenge **HAI lượt**: pha V (3–5 câu khó nhất, trước khoá scope) và pha I (trước dòng code đầu). Mình chỉ có lượt thứ hai. Hai lượt bắt hai loại lỗi khác nhau và **lượt tài liệu đắt hơn hẳn**: challenge-trước-code bắt "tôi đọc hiểu lệch", KHÔNG bắt được "tài liệu này chưa hề nói tới chuyện đó" — mà khoá scope xong thì mọi lỗ tài liệu phải trả bằng code sai. Luật chấm: trả lời **CHỈ bằng những gì đã ghi trong tài liệu**; câu nào phải viện kiến thức ngoài hay phải đoán = **một lỗ tài liệu**, vá rồi mới chấm PASS. Đòi ≥3 vì một câu dễ trúng chỗ mình vốn đã nắm, ba câu mới buộc đi tìm chỗ mình chưa chắc. `challenge-log` +cột `Giai đoạn` (`tài liệu`/`code`) để hai gate lọc riêng. **(2) `decisions_min` @`/approve-document` + mốc wave** — `decide.py` tồn tại mà **KHÔNG chỗ nào đòi**: một lượt làm tài liệu trọn vẹn (khám phá → nghiệp vụ → thiết kế → chia wave) không ghi nổi hai chỗ mơ hồ nghĩa là đã ĐOÁN IM LẶNG; sổ rỗng không chứng minh được là không có mơ hồ, chỉ chứng minh không ai ghi. Kèm `mark_decisions()` @`/next-wave` ghi mốc `quyết định của wave-N nằm DƯỚI dòng này` — thiếu mốc thì hai quyết định của wave 1 xanh hộ mọi wave về sau (đúng cơ chế mốc `(vòng N)` của VIPER, cùng họ với dấu wave ở `test_result`). **(3) `tracking/blockers.md` + `open_blockers()`** — VIPER có BA chỗ dừng hợp lệ; `NON_NEGOTIABLES` của mình chỉ nhắc hai (mơ hồ→`decide.py`, ngoài scope→wave sau), thiếu hẳn **chặn cứng**. Agent gặp tắc thật không có nước đi hợp lệ nào ngoài đứng lại hỏi. Nay: ghi một dòng → **chuyển việc khác NGAY** → báo gộp cuối lượt; cột `Đã thử gì` trống thì chưa phải blocker, mới là chưa thử đủ. Sổ không reset khi mở wave; `/next-wave` đếm dòng `mở` và **nhắc, không chặn** (cam kết scope mới trên nền còn tắc thì wave mới tắc theo). **(4) `MANUAL_CHECKS` + in "Không kiểm tự động được — tự xác nhận"** — gate của VIPER luôn in phần máy mù; gate mình im lặng về chỗ nó không kiểm được, mà **chốt xanh đọc như một lời bảo đảm** trong khi nó chỉ bảo đảm phần máy đếm được. Giấu chỗ mù đi thì không ai đi làm nốt. 5 chốt có mục tự-xác-nhận (approve-document: *đã ĐỌC chứ không chỉ bấm duyệt*, *đã MỞ mockup bằng trình duyệt* · dev-handoff · test-execute · dogfood: *dấu hiệu giả — cả 6 lăng kính báo không thấy gì ngay lượt đầu* · end-wave). **CÒN THIẾU, chưa port:** sổ đo + pha đối chiếu giả thuyết (`hypothesis-log` khai `TESTABLE→PROVEN` mà không gì đo) · hook soi nội dung lúc ghi file (guard_ds) · hợp đồng lệnh thống nhất · báo cáo rác tài liệu (compact.py). 14 phép kiểm mới. selftest 10/10. |
| 2026-08-22 | dọn rác: gỡ bộ máy bug end-to-end + template mồ côi | User: *"mấy cái template dọn đi rác quá, tài liệu thừa do những lần trước làm sai thì cũng xoá"*. **(1) Bộ máy bug — gỡ hẳn, không thay bằng gì.** Lý do: kết quả test vốn đã nằm ở `test-report.md`, `bugs.md` là **bản sao thứ hai của cùng một sự thật** phải tự tay giữ đồng bộ — mà hai bản sao thì sớm muộn lệch nhau, rồi `no_open_bugs` và `test_passed` cãi nhau. Nó cũng là chỗ **hành lang gãy nhịp**: `/run-wave` đang chạy một mạch thì phải rẽ sang chốt `fix-bugs`, tiêu một evidence `bug_id` không ai đọc. Gỡ: 2 transition (`log-bug`, `fix-bugs`) khỏi STATE-MACHINE (39→37) · gate `no_open_bugs` + `check_no_open_bugs` + 2 parser `_bugs_open_from_*` + selftest của chúng · `log-bug-agent` · skill `bug-logging` · `TEMPLATE.bugs.md` · builder `fix-bugs`/`log-bug` trong `build_prompt` · cờ `--bug-id`/`--description`. **Thay chỗ:** chốt `fix` mới (`build_prompt.py fix --tc TC-NNN --boundary <b>`) đọc **row TC trong `test-report.md` + nguyên nhân trong `test-logs/<TC>.log` + FEAT/AC trong registry** — không state transition, không sổ phải đóng bằng tay: sửa xong MAIN chạy lại chốt `test-execute`, report tự xanh. Mode A của `_template-fix-agent` đổi nguồn từ `BUG-NNN` sang TC fail. **Thứ KHÔNG được mất khi gỡ**, và cách giữ: (a) *"FAIL rồi để đó, không ai biết vì sao"* — `test_evidence` trước đây đòi mỗi FAIL có một row bug reference, nay đòi **log của chính TC đó đọc ra được NGUYÊN NHÂN** (error/exception/assert/status), kèm §"Log của một FAIL" trong skill test-execute liệt kê 6 vế phải ghi (boundary · tầng hỏng · TC+AC · reproduce · expected vs actual · status+stack) — thiếu vế nào thì lượt sửa phải đoán lại vế đó; (b) *"còn lỗi thì không được đóng wave"* — `test_passed` (derive từ report) đã chặn, chặt hơn `no_open_bugs` vì không phân biệt nặng nhẹ; (c) *"phát hiện dùng thử phải có quyết định"* — bảng `Xử` ở `dogfood-report.md` §2 (land phiên trước). Ranh giới giờ sạch: **`test-report` = kết quả máy chạy ra · `dogfood-report` = phát hiện + quyết định xử · `review-findings` = finding trước bàn giao** — ba sổ, ba loại sự thật, không chỗ nào chép chỗ nào (bảng ở `tracking/README.md`). **(2) Template mồ côi:** `TEMPLATE.local-dev.md` (0 tham chiếu, đã bị skill `infra-local-dev` thay) · `TEMPLATE.cr.md` (gỡ cùng `apply-cr` phiên trước). Còn 10 template ở `tracking/_templates/`, tất cả đều có chỗ gọi. **(3) 60+ chỗ nhắc bộ máy bug** trong 25 file (router · lệnh · PROTOCOL · agent · skill · template · frontmatter `description` mà Claude Code đọc để gợi ý skill) — dọn hết; `doc_integrity` xác nhận 0 chỗ trôi. selftest 10/10 (smoke 35/35 sau khi bỏ bước `fix-bugs`). |
| 2026-08-22 | khép 3 món VIPER còn lại + dọn code chết trong `scripts/` | **(1) Vòng giả thuyết — khép được nửa nằm trong tầm tay.** `hypothesis-log` khai vòng đời `TESTABLE → PROVEN | DISPROVEN` mà chuỗi `PROVEN` chỉ xuất hiện đúng một lần trong cả repo: ở chính template khai ra nó. Không gì đo → không gì chuyển trạng thái → sổ giả thuyết là **danh sách phỏng đoán không ai đối chứng**, một tài liệu trông như đang gác mà không gác gì. Harness dừng ở `/next-wave`, chưa có production nên KHÔNG thể đòi "đã đo"; nhưng ba thứ nằm trong tầm tay lúc khoá scope thì đòi được: **Ngưỡng bằng SỐ ghi TRƯỚC** (*"tăng đáng kể"* không bác bỏ được gì — giả thuyết nào cũng đúng nếu ngưỡng viết sau khi nhìn số; có số thì mới sai được, mà sai được mới là giả thuyết) · **Cách đo trỏ tới thứ có thật** (event ở PRODUCTION-READY nhóm 4 / một truy vấn cụ thể — không cài chỗ đo thì ngưỡng chỉ là số trang trí) · **Wave đo**. Gate `hypothesis_measurable` @`/approve-document`. Nửa sau: `unconcluded_hypotheses()` @`/next-wave` hỏi lại giả thuyết khai đo ở wave vừa đóng mà còn `TESTABLE` — **nhắc, không chặn**, và `CHƯA ĐỦ DỮ LIỆU` là **kết luận hợp lệ** (ép ra PROVEN/DISPROVEN khi số chưa đủ là đóng một câu hỏi vẫn đang mở). Template +cột `Ngưỡng`/`Wave đo`/`Số thật`. **(2) `token_violation()` — hook soi NỘI DUNG lúc ghi file** (port `guard_ds`). Luật "mockup và FE chỉ lắp từ token" trước đây chỉ sống ở gate `web_styling` @dev-handoff: **báo sau khi code đã viết xong**, và chỉ khi có người chạy tới chốt đó. Nay chặn ngay lúc ghi `docs/architecture/ux/mockups/**` + `services/**/*.{css,scss,html,tsx,jsx,vue,svelte}`: mã màu thô ngoài `:root` · `var(--x)` chưa khai ở `design-tokens.css`. **CHỈ chặn lỗi MỚI so với bản trên đĩa** — file bẩn từ trước là việc của gate; edit không thêm lỗi (kể cả edit đang sửa dần từng lỗi) phải đi qua, nếu không hook bắt đền lỗi cũ và Edit không sửa nổi file. Fail-open có địa chỉ: chưa chốt design-tokens · SoT chưa khai token nào · file ngoài phạm vi → cho qua, `web_styling` + `design_system_closed` là backstop. 9 phép kiểm. **(3) `doc_integrity` +phép soi TEMPLATE MỒ CÔI** — template không ai gọi là chỗ trốn kỹ nhất của rác: không gây lỗi, không ai xoá, lần sau có người chép nhầm bản đã chết. Phép kiểm bắt ngay `TEMPLATE.blockers.md` vừa tạo (tôi trỏ `tracking/blockers.md` chứ chưa trỏ tên khuôn) → nối vào NON_NEGOTIABLES + router. **(4) KHÔNG port hợp đồng 6 lệnh (`make dev/check/test/…`)** — đo trước khi làm: chỗ rẽ nhánh theo kind trong kernel là **đúng MỘT hàm** (`_build_test_cmd`, 14 dòng). VIPER cần Makefile vì stack của họ mở; mình có 4 kind cố định. Thêm một tầng gián tiếp để gỡ 14 dòng là làm nặng lên chứ không gọn đi. **(5) Dọn code chết** (user: *"toàn thêm mà chưa thấy xoá"*): `HARNESS_CMD_RE` — hai regex trùng chức năng, bản có `(?:complete\s+)?` không ai gọi, gộp còn một · `DOMAIN_PO_MODES`/`DOMAIN_BA_MODES` — tàn dư hệ mode bỏ từ lúc gộp `/domain` · `harness_lib.load_yaml`/`save_yaml` — 0 chỗ gọi, kéo theo dependency `pyyaml` không cần. Quét 34 "hàm chết" thì **31 là dương tính giả** (dispatch qua dict `BUILDERS`/`HANDLERS`/`GATES`) — kiểm từng cái trước khi xoá, không tin grep. selftest 10/10. |
| 2026-08-22 | `bootstrap.py` — dựng project mới bằng COPY-RA + `selftest_all.py` tự dò + sửa bug reset xoá nhầm agent | **(1) `bootstrap.py`.** Cách bắt đầu project mới trước đây là fork repo rồi tự chạy `reset_for_new_project.py` trong chính bản fork — ba chỗ hụt: **bản khung bị dùng làm chỗ làm việc** (chạy thử một wave là nó bẩn; `archive/wave-001/` từng lọt vào repo khung và thư mục đó **kiêm cờ "wave đã đóng"**, nên mọi fork sau sinh ra đã đóng sẵn wave 1) · **lịch sử git của khung đi theo** (project mới `git log` ra 200 commit về cái khung) · **phải NHỚ chạy reset** (quên là khởi đầu với tài liệu project khác). Đổi sang mô hình COPY-RA của VIPER: khung nằm im, mỗi project một thư mục, `git init` sạch. Bước dọn **gọi lại `reset_for_new_project.py`** trong đích chứ không chép logic sang — nó biết chính xác cái nào là instance (PROJECT.md, FEAT-*, hld-*, wave-*) và cái nào là khuôn; chép sang là đẻ bản sao thứ hai rồi lệch. Không copy: `.git` · `archive/` · `services/` · zip · `HARNESS-CHANGELOG.md` (nhật ký của KHUNG, không phải của sản phẩm) · `settings.local.json` · chính `bootstrap.py`. Kiểm sau khi dựng: file bắt buộc đủ · STATE hợp lệ · stage=BOOTSTRAP · `archive`/`services` rỗng. **(2) `selftest_all.py`** — danh sách phép kiểm nằm trong đầu người gõ: đo được **10 script có `--selftest` mà bộ đang chạy chỉ đụng 7** (`capture_feature_state`, `capture_infra_proof`, `materialize_matrix` có phép kiểm chạy được mà **không ai gọi**, hỏng cũng không ai biết). Cùng bài với `gates.py --list`: thay chép tay bằng **dò**. Kèm phép soi **LỌT LƯỚI** (có `_selftest` mà không khai `--selftest`) vì `gates.py` — file lớn nhất repo — lọt đúng khe đó: nó chạy selftest MẶC ĐỊNH nên trong file không có chuỗi `--selftest`. *Một luật dò im lặng bỏ sót còn tệ hơn danh sách chép tay: danh sách ít ra còn nhìn thấy là thiếu.* 15 phép, tất cả xanh. **(3) Bug thật do bootstrap lộ ra:** dựng project mới xong thì thiếu 2 agent. Truy ra `reset_for_new_project` xoá "mọi `*-agent.md` KHÔNG có trong `CORE_AGENTS`" — danh sách trắng chép tay **thiếu `domain-translator-agent.md` và `ux-designer-agent.md`**, nên **ai chạy reset là mất hai agent singleton, âm thầm**. Đảo chiều fail-safe: nhận diện bằng **hình dạng tên** materialize (`^(dev|fix|review)-{prefix}-{boundary}-agent\.md$`) → khớp thì xoá, còn lại GIỮ. Agent lạ sót lại thì dọn tay được; agent singleton bị xoá nhầm thì phải đi đào git. **(4) Hai lỗ nhỏ cũng do bootstrap lộ:** `doc_integrity` tính **changelog là tham chiếu sống** (từng-có ≠ đang-dùng) → loại nó khỏi corpus; và phép soi template mồ côi **báo oan dạng gộp** `TEMPLATE.service-repo-{CLAUDE.md,settings.json,gitignore}` → nhận thêm tiền tố đứng trước `{`. **(5) `scripts/README` liệt kê 12/23 script** — bản chép tay thiếu quá nửa mà không gây lỗi nên không ai thấy; viết lại đủ 24 dòng. README + SETUP-GUIDE đổi từ "fork + reset" sang `bootstrap.py`, và gộp danh sách kiểm 4-5 lệnh thành `selftest_all.py`. Project dựng thử: 176 file, 15/15 selftest xanh, 30 agent đủ. |
| 2026-08-22 | tìm ra GỐC của "quét mãi vẫn sót": phép soi lệnh ma mù ba chỗ — vá rồi dọn 400+ tham chiếu | Sau nhiều lượt "đã dọn sạch" mà vẫn lòi tàn dư, truy ra không phải do dọn ẩu mà do **công cụ soi mù**, nên mỗi lượt đều báo sạch thật lòng. Ba lỗ, mỗi lỗ che một lớp: **(1) Lookbehind loại dấu backtick.** `SLASH = r"(?<![\w`/-])/(...)"` — ký tự `` ` `` nằm trong tập loại trừ, mà `` `/apply-cr` `` trong nháy ngược là **cách viết phổ biến nhất** của lệnh trong tài liệu. Gỡ backtick ra khỏi lookbehind → từ "0 chỗ trôi" nhảy lên **197 chỗ trong 31 file**. Một ký tự che 197 chỗ. **(2) `SURFACES` không có `docs/**`, `tracking/**`, `scripts/**`.** Bản trước chỉ soi router + lệnh + skill + agent. 20 chỗ `apply-cr` sống trong template `docs/` qua mọi lượt quét — và **template là thứ project mới CHÉP RA DÙNG**, lệnh ma ở đó lan sang mọi project sau. Mở rộng, kèm phân biệt: `.py` soi HẸP (chỉ lệnh ma; `gates.py` là nơi ĐỊNH NGHĨA gate, soi tên gate ở đó chỉ đẻ dương tính giả), `.md` soi đủ; bỏ qua chính `doc_integrity.py` (nó buộc phải chứa cả danh sách RETIRED). **(3) Luật bỏ-qua-dòng-migration quá rộng.** Chuỗi con `"| \`/"` nuốt mọi bảng có lệnh ở **cột THỨ HAI** — `agents/README.md` có 14 dòng như vậy. Đổi sang regex neo đầu dòng, nhận cả cột đầu gộp nhiều tên (`| \`/design\` · \`/plan\` | … |`). **Dọn theo báo cáo mới:** 400 chỗ `/`+tên-chốt bỏ dấu gạch chéo trên 90 file (chốt KHÔNG phải lệnh người gõ — sub-agent đọc `/dev-handoff` thì tin là có lệnh đó để gọi) · **`apply-cr` gỡ hết**, gồm cả **`state.py` còn nguyên nhánh `elif command == "apply-cr"`** — code sống cho một lệnh đã chết — và khối `change-requests/` trong `reset_for_new_project` · `TEMPLATE.cr.md`, `log-bug-agent.md` khỏi `CORE_AGENTS`, 3 link chết. Mọi chỗ "breaking → `/apply-cr` từ DONE" viết lại thành "**breaking → WAVE SAU**: lùi `/domain` sửa hợp đồng, `/approve-document` khoá lại, wave kế giao bản mới; consumer đang chạy phải còn chạy được suốt lúc đó". Sau cùng: khung sạch 0 chỗ trôi, 15/15 selftest; project dựng bằng `bootstrap.py` cũng 0 chỗ trôi + 15/15. |
