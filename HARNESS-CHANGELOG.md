# Harness Change Log

> Nhật ký thay đổi kernel/gate/skill của ADLC Design Harness (append-only). Tách khỏi CLAUDE.md để router gọn — CLAUDE.md chỉ giữ trạng thái HIỆN TẠI, file này giữ LỊCH SỬ.
> Mỗi thay đổi non-trivial (gate mới, đổi flow, vá bug enforcement) thêm 1 row `| Date | CR/ADR | Summary |` ở CUỐI. Mới nhất ở dưới cùng.

| Date | CR/ADR | Summary |
|---|---|---|
| 2026-05-29 | — | Initial CLAUDE.md (router style v4 rebuild) — polyrepo, 10 states, 13 commands |
| 2026-06-13 | — | Clone DISCOVERY (D0-D3 ideation) từ ADLC: +4 state `DISC_D0..D3`, +2 command `/discovery-start`,`/discovery-end`, +gate `discovery_gate.py`, +docs/discovery/*. |
| 2026-06-13 | — | Tách intake hoàn toàn (bỏ `/intake-requirement`), khớp cách chia nhỏ của ADLC: front-half = DISCOVERY(D0-D3) → DOMAIN_AUTHORING → DESIGN → PLAN → REVIEW (rename từ INTAKE). DOMAIN author Epic/Feature/BR **thẳng vào `docs/architecture/{epics,feat,business-rules}`** (1 stage, no translate/sync/docs-domain — đó là plumbing multi-repo); skill domain-po (EPIC/FEATURE) + domain-ba (BR) + agent; template ZIP. DESIGN=`/design` (technical-design), PLAN=`/plan` (implementation-plan) tách stage riêng, gate force-capable (design_gate/plan_gate). MATRIX materialize ở PLAN (ALLOW_STAGES). apply-cr → DESIGN. |
| 2026-06-13 | — | **Audit sync ADLC→harness (fix drift)**: sửa số liệu IDENTITY (10→17 states, 14→19 commands); cập nhật tham chiếu chết (intake step3/4, `/intake-requirement`) → DESIGN / Discovery D3; bổ sung mode `JOURNEY`+`PERSONA` vào chữ ký `/domain-start` ở CLAUDE.md + đồng bộ `commands/domain-start.md` (frontmatter, bước 1&5, dòng cuối); bỏ `WIREFRAME` (mode đã xoá) và `DOMAIN_TRANSLATING`/`TRANSLATE` (plumbing multi-repo đã bỏ) trong message "Sau D3" của `build_prompt.py`. |
| 2026-06-13 | — | **Quyết định: KHÔNG clone các tính năng ADLC sau** (single-repo nên không cần / hoãn): SPECS hub + translate/sync + ORCHESTRATOR UI + repo UIUX/BOUNDARIES/EXPERIENCES tách rời (đã bỏ có chủ đích từ đầu). **Hoãn (gap đã nhận diện, chưa làm)**: (1) state `BLOCKED` + `/blocker-raise`/`/blocker-resolve` (escape hatch khi agent kẹt); (2) design-system/design-tokens/component-library dùng chung (hiện chỉ UX per-boundary qua `ux-design`); (3) `/cr-raise` giữa luồng (hiện chỉ `/apply-cr` từ DONE); (4) `/import-baseline` brownfield Wave 0; (5) qa-translator re-map TC khi AC đổi + dedupe-registry; (6) governance util `/decision-log`,`/memory-audit`,`/reset-question-budget`,`/scope-extend`; (7) doc tổng quan system-level (SYSTEM-ARCHITECTURE/SYSTEM-TOPOLOGY/CONTRACT-MAP) — hiện rải ở HLD per-boundary + BOUNDARY-MAP. Cân nhắc bổ sung khi có nhu cầu thực tế. |
| 2026-06-14 | — | **DESIGN tách self-loop + `/design-end`** (19→20 command): `/design` giờ `DESIGN→DESIGN` (thảo luận/refine, KHÔNG advance), `/design-end` `DESIGN→PLAN` (verify gate, không spawn author mới); per-role docs-to-read tối ưu. **3 gate single-repo** chắt lọc ZIP per-target MANIFEST: `design_gate` per-boundary completeness (backend/bff→hld+api, web/mobile→hld+ux), `plan_integrity` (FEAT-id MATRIX có file + depends_on no-cycle/no-dangling), `matrix_coherence` (MATRIX phủ đủ boundary đúng kind) + `planning_lint` ref-integrity mở rộng journey/persona, persona-ref chuẩn-hoá file-id `PERSONA-{prefix}-NNN` (bỏ P-id pool). **23 template** chuẩn-hoá theo ZIP rồi trim lean. |
| 2026-06-14 | — | **`apply-cr` re-enter `DOMAIN_AUTHORING`** (was DESIGN) — vá gap mở-rộng-feature-sau-ship: CR thêm feature → `/domain-start` author epic/feat/BR rồi chảy DOMAIN→DESIGN→PLAN→REVIEW→start-wave; CR kiến trúc-only → `/domain-end` qua thẳng. Boundary MỚI vẫn dùng done-wave→`/discovery-start D3` (discovery re-runnable idempotent vì done-wave giữ docs). smoke +1 step (apply-cr→DOMAIN). |
| 2026-06-14 | backlog G12+G13+G1 | **Back-half honest-gate (rút từ e2e ClinicBook — META "giàu spec nghèo enforcement máy"):** (G13) `scripts/capture_infra_proof.py` HARNESS tự curl `/health/ready` → `health-proof.json` + gate `health_proof` @dev-handoff/test-plan (State=running chưa đủ; app phải trả 2xx); (G12) gate `test_evidence` @test-execute parse test-report+test-logs (auto-TC in-scope phải có network-call thật, cấm silent-skip) + **DERIVE `test_result`** từ report (không tin agent tự khai); (G1) deferred-scope: `wave-{N}.md §6 Deferred` là SoT → test-plan tag `@deferred` → test-execute skip(deferred) không log bug → `/end-wave` close sạch tự nhiên, hết ép `test_result=pass`. Tag không khai báo wave plan → coi in-scope (chống né test). Gate selftest hermetic + smoke 30/30. |
| 2026-06-16 | nhận-xét #2+#3+#9 | **DOMAIN 2-lớp business↔eng + KÝ + TRANSLATE (clone ZIP, adapt single-repo) + E-6 chặt.** (A1) lớp BUSINESS plain VN `docs/domain/{epics,feat,journeys,business-rules,personas}` (template clone ZIP, có "Câu hỏi cho Author") — `/domain-po`(EPIC/FEATURE/JOURNEY) + `/domain-ba`(BR/PERSONA) author (loop tới khi OK + hỏi-author-questions ngay, status DRAFT). (#3) `/domain-approve <id|all>` KÝ (`status: APPROVED`, ký TRƯỚC dịch SAU) — `domain_approve.py` jargon-check + stamp; gate `domain_no_jargon`. (#2) `/domain-translate` spawn `domain-translator` (clone ZIP agent: DỊCH-KHÔNG-sáng-tác, field kỹ thuật = TODO-engineer) dịch `docs/domain/`→`docs/architecture/` eng; gate `domain_signed` (mọi doc ký). Bỏ SPECS-hub/cross-repo-sync. command 20→23 (bỏ `domain-start`, +`domain-po`/`-ba`/`-approve`/`-translate`); back-edge DESIGN→DOMAIN qua po/ba; phase-lock +`docs/domain/`. (#9/E-6) hook chặn spawn tự-viết-tay MỌI workflow agent: detect theo **registry tên-agent** (`agents/*.md`) + keyword → thiếu chữ ký build_prompt = block. gates+policies selftest + smoke 35/35 + state validate + parity xanh. |
| 2026-06-16 | nhận-xét #1 | **Discovery: gộp cơ chế start D0→Dn + complete-before-spawn.** `/discovery-start` giờ TIẾN qua wave (nhảy tiến `D{N}→D{N+1}` = gate wave hiện tại qua `discovery_advance`; gọi lại cùng wave = refine, no gate); bỏ 3 transition `discovery-end` tiến-wave, `/discovery-end` chỉ còn 1 (DISC_D3→DOMAIN, không arg). **complete-before-spawn**: command transition STATE NGAY (trước build_prompt+spawn) → STATE đúng stage để phase-lock cho agent ghi `docs/discovery/**` + next-step/Stop-hook đúng (giống `/start-dev`). `find_transition` thành **evidence-aware** (phân biệt refine vs advance cùng `(from,trigger)`). gates selftest + smoke **32/32** + state validate + parity xanh. |
| 2026-06-16 | nhận-xét #6+#7+#8+#10+#11+#12+#13+#14 | **Batch dev-quality + flow-control (từ chạy thật tới test-execute):** (#6) entity Java → `{Resource}Entity` + folder `entities/` (rules-backend + ref-backend-pattern). (#7) **Gradle default** mọi backend (`build.gradle` Groovy DSL; Maven opt-in qua ADR) — rules/ref/review/template + dispatcher `_build_test_cmd` Gradle-first. (#8) review-backend §L2 ép check tuân-thủ `rules-backend` + ref-skill (cấm **FQCN** inline — phải import) + §N schema-drift (entity↔migration qua Testcontainers `ddl-auto:validate`). (#10) `/review-dev complete{}` ép `review_results` (gate `non_empty`) chống STATE rỗng kẹt dev-handoff; `all_boundaries_reviewed` thêm force-bypass. (#11) **turn-flag**: 1 `harness complete`/user-turn (marker `harness/.turn-advance.flag`, reset UserPromptSubmit/SessionStart) — complete thứ 2 deny "MAIN tự nối lệnh"; gate-fail KHÔNG tiêu cờ. (#12) dev-handoff **INFRA-ONLY**: `_pre_task` set `spawn.active=dev-handoff-agent` → `PreToolUse(Write\|Edit)` block `services/**` (FM-HANDOFF-NO-CODE-FIX) — lỗi code boundary → fix-agent, không tự vá. (#13) test-plan + test-execute trỏ template `tracking/_templates/TEMPLATE.test-case-registry.md`. (#14) test-execute SEED test-data (cột `test-data`/`pre-condition` đủ cụ thể) trước chạy + cleanup. gates+policies selftest + smoke 35/35 + parity. |
| 2026-06-21 | service-lifecycle wave | **End-wave TẮT service + handoff reuse rõ ràng.** (1) `/end-wave` thêm `docker compose stop` (dừng container, GIỮ image+volume) — UAT đã xong ở MANUAL_TEST (handoff §5 sửa nhãn "sau end-wave"→"ở MANUAL_TEST trước end-wave") nên không cần service chạy; trước đây giữ UP "post-mortem" (lý do yếu). KHÔNG `down --volumes` (vẫn là việc `/done-wave`). build_end_wave + end-wave-agent (+skill infra-local-dev, +`infra_stopped`) + command + handoff §8. (2) Handoff wave kế **tái dùng** service wave trước: `docker compose up -d --build` idempotent — image+volume từ `end-wave stop` giữ nguyên → khởi động NHANH, chỉ boundary mới/đổi mới build; KHÔNG `down` trước (ghi rõ ở build_dev_handoff). smoke 35/35 + selftest + parity. |
| 2026-06-21 | turn-flag auto-mode hole | **Vá MAIN tự nối slash-command ở auto mode.** Lỗ hổng: turn-flag (#11) reset ở `UserPromptSubmit`; khi MAIN TỰ invoke slash-command kế (Skill/SlashCommand tool) để chạy `/test-plan`/`/test-execute`… → fire lại `UserPromptSubmit` → xoá cờ → `harness complete` kế lọt → nối trọn pipeline (auto mode không có ma sát phê duyệt). Fix: thêm `PreToolUse(Skill\|SlashCommand)` hook (`dispatcher._pre_skill`) chặn MAIN gọi tool chạy lệnh ∈ `GATE_RULES` (deny tại NGUỒN, trước khi slash-command reset cờ); lệnh user GÕ tay pre-loaded → không qua tool → không ảnh hưởng; skill ngoài-harness cho qua. +matcher `Skill\|SlashCommand` ở `.claude/settings.json`. dispatcher selftest + gates/policies selftest + smoke 35/35 + parity. |
| 2026-06-21 | backend-lombok-consistency | **Vá mâu thuẫn ref backend ↔ hub (dev+review hiểu sai `@RequiredArgsConstructor`/`@Slf4j`):** `rules-backend` (hub) ĐÚNG bắt `@RequiredArgsConstructor` + `@Slf4j`, nhưng VÍ DỤ CODE ở ref skills lại viết NGƯỢC (constructor tay `Impl(...){this.x=x;}` + `LoggerFactory.getLogger(...)` thủ công) → dev copy ref → sai, review không có mục bắt → lọt. Sửa ví dụ cho khớp hub ở `ref-backend-pattern` (service), `ref-backend-logging` (`@Slf4j`, bỏ LoggerFactory), `ref-backend-redis` (RedisServiceImpl), `ref-backend-kafka` (4 bean: publisher/command-service/relay/consumer); thêm forbidden `LoggerFactory.getLogger` thủ công. `review-backend §L2` thêm mục bắt **DI manual-constructor/`@Autowired`-field + manual-LoggerFactory = MAJOR**. Constructor tay hợp lệ giữ nguyên (enum `OrderErrorCode`, exception `BusinessException`); component stateless (interceptor/mapper) không cần annotation. |
| 2026-06-21 | E-6 test-spawn | **E-6 keyword phủ test-plan/test-execute (vá MAIN tự build prompt spawn test).** `DEV_SPAWN_KEYWORDS` += `test-plan`, `test-execute` → `PreToolUse(Task)` block spawn 2 agent này bằng prompt tự-viết-tay (trước đây chỉ nhánh tên-agent `test-*-agent` chặn → MAIN né bằng cách không nhắc tên agent). build_prompt output (`# SPAWN PROMPT`) vẫn pass; space-form "test plan" KHÔNG false-positive. policies selftest + smoke 35/35 + parity. |
| 2026-06-16 | nhận-xét #15 | **Gate `doc_review` @ `/approve-document` — doc-review 2 mode.** `/review-document` **CÓ arg** = revision (sửa theo comment user, vòng đã có); **KHÔNG arg** = sanity-check soi 5 lens (độ-phủ năng-lực `capability-map`+persona+journey→FEAT; mâu-thuẫn cross-doc; AC testable; cross-ref; câu-hỏi-author chưa chốt) → ghi `tracking/doc-review-findings.md` (`DR-NNN` + severity, template `tracking/_templates/`). Gate `check_doc_review`: thiếu file (chưa chạy sanity-check) HOẶC còn gap **BLOCKER/MAJOR** open → **chặn approve** (mirror `review-dev no_open_findings` cho TÀI LIỆU; bắt **thiếu năng lực nền vd auth/login** trước build). Lens đặt ở build_prompt + `review-document-agent` (KHÔNG đụng skill `business-analysis`). force-bypass+audit `decisions.md`. gates selftest +doc_review hermetic + smoke 35/35 + parity. |
| 2026-06-14 | — | **Doc phase-lock + back-edges + next-step hint (vá 2 gap user: hook hổng + báo-bước-tiếp mơ hồ):** (#2) port ZIP `pretooluse-readonly-inputs.py` sang single-repo = `policies.phase_lock_violation` ở `PreToolUse(Write|Edit)` — doc discovery/domain/design/plan chỉ sửa ở stage sở hữu (+REVIEW), stage sau frozen (NON-NEGOTIABLE #6 từ honor-system → hook-enforced; chống dev/test sửa spec khớp code + sửa FEAT/HLD lúc ở PLAN). Sửa upstream = **back-edge** `PLAN--/design-->DESIGN`, `DESIGN--/domain-start-->DOMAIN` (dùng lại lệnh entry, không command mới → parity giữ; tiến lại re-gate) hoặc `/apply-cr` sau ship. (#1) next-step hint contextual (`policies.STAGE_NEXT_GUIDE`: lệnh+arg+nghĩa+back-edge) thay list tên trống. policies selftest + smoke 32/32 (+2 back-edge) + state validate + parity. ZIP translate (plain↔eng DOMAIN→SPECS) giữ nguyên BỎ (single-repo author thẳng). |
| 2026-06-14 | backlog G15-now | **UI design-fidelity deterministic (e2e obs #6, Figma defer):** artifact dùng chung `docs/architecture/ux/design-tokens.css` (SoT `--color-*`/`--font-*`/`--space-*` + dark/hc theme, `TEMPLATE.design-tokens.css`) — mọi web boundary consume, không bịa palette per-boundary; nâng gate `web_styling`: web style bằng plain CSS phải dùng design token `var(--...)` (tailwind/CSS-in-JS miễn) → chống "FE bịa màu/spacing rời design system". Skill ux-design + rules-web (rule 44) trỏ shared token. G15-future (Figma design-to-code + visual diff) DEFER. |
| 2026-06-14 | backlog G16 | **Wave-sequence validator (e2e obs #7 + #5 verify):** port `scripts/wave_sequence_lint.py` từ ZIP `wave-sequence-validate.py` (single-repo: bỏ contract-signing, `inherited_active`→file-exists) → gate `wave_sequence_lint` @plan: enum `wave_class`/`wave_strategy` + `target_count_per_layer ≤ 3` + strategy layer-purity (horizontal-be/-fe) + vertical `parent_epic` + `inherited_active` file tồn tại. Field §2 WAVE-SEQUENCE từ "trang trí" → được gate; bỏ chữ "chưa gate"/"forward-looking" ở template + implementation-plan SKILL. Selftest hermetic (module + wiring) + smoke 30/30 + parity. **+ #5 verify doc-contract:** thêm invariant ZIP `lint_execution` "FAIL phải log bug" vào `test_evidence`; deferred SoT 1 nguồn (handoff §6 → wave §6). |
| 2026-06-14 | backlog G11+G4/G6 | **Dev-quality + integration-realism gate (e2e obs #1,#2):** (G11) gate `code_compliance` @dev-handoff — backend boundary cấm H2 (`com.h2database`/`jdbc:h2`/`ddl-auto: create-drop`) + bắt `Dockerfile` + `application.{yml,properties}` (đối xứng `web_styling`, chặn "test xanh nhờ H2" + "dev-done ≠ runnable"); (G4/G6-A) gate `contract_test_present` @test-plan — consumer (có `depends_on` trong wave) phải có ≥1 auto-TC contract/integration/e2e nối tới (chống thiếu liên kết BE-FE → bug); (G6-B) gate `api_transport` @plan — api spec KHÔNG truyền tenant-id qua query string (phải `X-Tenant-ID` header/JWT claim, api template §2 — chống drift BUG-012). Tất cả force-bypass+audit; selftest hermetic + smoke 30/30. |
| 2026-07-04 | EXAMPLE → thư viện ARCHETYPE màn (generic mọi đề bài) | **Trả lời "example gắn 1 đề bài, đề khác theo sao?" (user):** cái cần neo không phải màn cụ thể mà là ARCHETYPE — app nghiệp vụ nào cũng ghép từ bộ hữu hạn. Viết lại `EXAMPLE.reference.html` thành gallery 6 archetype nội dung generic: 1 Dashboard (KPI cards) · 2 Bảng danh sách (toolbar+hover-row+badge+pagination) · 3 Form (2 cột→1 cột, lỗi cạnh field, actions phải) · 4 Trang chi tiết (header+badge+description list) · 5 Timeline/lịch tài nguyên (block span, ô trống im lặng) · 6 Feedback states (skeleton/empty/error-banner/toast/modal). Mỗi màn tương lai: xác định archetype (hoặc ghép) → so section đó. Skill/spawn-prompt/README cập nhật wording. |
| 2026-07-04 | EXAMPLE exemplar + luật màu neutral-first | **Root-cause "demo đẹp — chạy thật vẫn xấu" vòng 2 (screenshot: nền xanh pastel toàn màn, block hồng-đỏ):** chẩn đoán — (a) taste không truyền được bằng tính từ, LLM bắt chước VÍ DỤ tốt hơn nghe mô tả; (b) danh sách anti-pattern là đập chuột (agent sửa đúng 5 lỗi bị cấm, phạm lỗi mới); (c) có token nhưng thiếu nguyên tắc DÙNG màu. Vá: (1) **`ux/mockups/EXAMPLE.reference.html`** — bài mẫu chất lượng commit vào repo (exemplar đối chiếu, KHÔNG phải template điền; có :root fallback chỉ riêng file này để mở xem được trước khi project có design-tokens); skill/spawn-prompt BƯỚC 0 = mở bài mẫu, vẽ xong TỰ SO — thua = làm lại không trình user; (2) **LUẬT MÀU neutral-first**: nền luôn surface trung tính, CẤM sơn semantic lên mảng lớn (màu = nhấn ~10%), ô trống im lặng hover mới hiện affordance, booking = primary-soft (danger CHỈ cho lỗi); (3) phase-lock exempt `EXAMPLE.*` (policies + selftest). |
| 2026-07-04 | chuẩn chuyên nghiệp UI + ui-kit library | **Vá "demo đẹp mà chạy thật xấu hoắc" (screenshot user: bảng text-link 'Trống'×50 ô, link gạch chân thay button, booking không span):** (1) ux-design +§Chuẩn chuyên nghiệp & ANTI-PATTERNS đích danh (benchmark SaaS thương mại; cấm text-link lặp mọi ô — ô trống im lặng chỉ hover mới hiện affordance, cấm link thay button, booking = block span theo thời lượng, cấm bảng HTML mộc, lỗi/toast đúng chỗ, responsive kiểm thật); build_design_ux nhấn "phạm anti-pattern = làm lại, không trình user". (2) **ADR ui-kit chốt component library trưởng thành** (React → khuyến nghị Ant Design 5): mockup MÔ PHỎNG visual language của library (HTML tĩnh không nhúng antd được), dev dùng library THẬT + design-tokens map qua theme (ConfigProvider) — mockup↔app hội tụ; technical-design (ADR bắt buộc khi có FE) + rules-web 2 nhánh (library vs plain-CSS) + review-web check đúng ui-kit. (3) gates `_web_styling_signals` +`_UI_LIB_MARKERS` (antd/@mui/chakra/primereact): app dùng component library = styled hợp lệ + miễn var(--...) như tailwind (trước bị chặn oan). Selftest +antd case; smoke 36/36. |
| 2026-07-04 | màn tuân thủ FEAT (SCREEN-MAP ↔ feat 2 chiều) | **Design theo màn nhưng FEAT là khuôn (góp ý user):** `_screen_map_problems` +2 check khi `docs/architecture/feat/` đã author — (1) FEAT-id trong cột `feat` của row phải có file thật (màn phục vụ feature ma = chặn); (2) chiều ngược: MỌI FEAT `has_ui_touchpoint: true` (opt-out status dùng CHUNG `planning_lint._FEAT_OPTOUT_STATUSES` — không chép tay) phải xuất hiện ở ≥1 row (feature có UI mà 0 màn = bị bỏ rơi khỏi design = chặn). Hermetic selftest +3 case. Cùng họ traceability với ac_coverage (AC↔TC) nhưng cho lớp DESIGN (FEAT↔màn). |
| 2026-07-04 | thiết kế theo MÀN + SCREEN-MAP | **Đơn vị thiết kế = MÀN, không phải boundary (góp ý user):** nhiều FE boundary thì phải gắn RÕ màn nào thuộc boundary nào + đọc tài liệu nào. Thêm **`ux/SCREEN-MAP.md`** (ux-designer sinh TRƯỚC khi vẽ): bảng `screen|route|boundary|feat|mockup|note` — luật gán màn→boundary: FEAT `target_experience_hint`/`target_boundary_hint` → persona của experience (BOUNDARY-MAP/CHARTER) → mơ hồ HỎI user (không đoán); mỗi FEAT has_ui_touchpoint ≥1 màn; màn dùng chung = 1 row nhiều FEAT. Khi vẽ 1 màn chỉ đọc tài liệu CỦA MÀN đó (FEAT:AC + api mà flow gọi). `design_gate` thay check per-boundary-dir bằng **parse SCREEN-MAP từng row** (`_screen_map_problems`, hermetic selftest): mockup phải TỒN TẠI + dùng token; màn gán boundary ma = chặn; web boundary 0 màn = chặn ("boundary trắng design"). ux-designer own +SCREEN-MAP.md; reset script dọn; routing table +3 câu hỏi màn/mockup. |
| 2026-07-04 | dọn template UX cũ (bỏ TEMPLATE.ux.md) | **Nhất quán hướng "thiết kế thẳng": xóa `TEMPLATE.ux.md`** (di sản thời wireframe — §4 bảng token còn CHÉP GIÁ TRỊ trùng design-tokens.css = drift magnet). `ux-{boundary}.md` giờ viết thẳng theo **outline trong skill ux-design** (Tổng quan · Flows · Screens[link mockup+states+API+validation] · Permission · Global patterns · A11y · Edge cases · Open questions), luật mới: **KHÔNG chép giá trị token vào .md** — design-tokens.css là SoT duy nhất. GIỮ `TEMPLATE.design-tokens.css` (không phải khung điền mà là palette seed — asset của hướng mới). Cập nhật refs: skill/agent/build_prompt/docs README. |
| 2026-07-04 | mockup không template — thiết kế thẳng | **Bỏ `mockups/TEMPLATE.mockup.html`** (user: "không cần template, thiết kế luôn giao diện bằng HTML"): skeleton copy-điền gò bó — ux-designer giờ **THIẾT KẾ THẲNG giao diện hoàn chỉnh** per screen như trang web thật (tự dựng app shell + nội dung thật theo §Visual polish). LUẬT giữ nguyên và chuyển về `ux/mockups/README.md` (HTML tĩnh file:// · chỉ `var(--...)` — thiếu token thì THÊM vào design-tokens.css chứ không bịa tại chỗ · không lorem · state phụ = section · responsive · đủ hover/focus). Gate `design_gate` không đổi (≥1 mockup/web boundary + dùng token). Cập nhật: skill ux-design, ux-designer-agent, build_design_ux, command design-ux, TEMPLATE.ux note, gates message, reset script (giữ README khi dọn). |
| 2026-07-04 | design bằng HTML (mockup thay ASCII) | **`/design-ux` chuyển từ ASCII wireframe sang MOCKUP HTML TĨNH** (user chê "vẽ xấu quá trời" — ASCII không duyệt look được): mỗi screen 1 file `ux/mockups/{boundary}/{screen}.html` theo `mockups/TEMPLATE.mockup.html` mới (app shell compose từ design token, mở `file://` xem được, KHÔNG JS/build/CDN, chỉ `var(--...)`, state phụ loading/empty/error = section, responsive media query, nội dung thật không lorem). Phân vai SoT: mockup = LOOK, `ux-{boundary}.md` = BEHAVIOR (bỏ hẳn wireframe ASCII khỏi TEMPLATE.ux). `design_gate` +2 check: mỗi web boundary ≥1 mockup non-TEMPLATE + mockup phải reference token (`_web_mockup_problem`, hermetic selftest). Dev FE (rules-web rule 1) bám mockup làm SoT look; review-web §6 đối chiếu app↔mockup (lệch rõ = MAJOR); reset script dọn `mockups/{boundary}/`. ux-designer-agent own `ux/mockups/**`. smoke 36/36. |
| 2026-07-04 | doc_stamped (status máy-hóa @approve-document) | **Vá "approve document rồi mà doc vẫn DRAFT" (user bắt được khi chạy thử):** `/approve-document` trước đây CHỈ set `approved: true` vào STATE — frontmatter `status:` của doc design/contract là field trang trí không ai flip (bệnh field-không-máy-hóa như wave_class trước G16). Thêm `scripts/approve_document.py` (idempotent): adr/hld/data-model/ux/integrations → `status: APPROVED`; api/events (contract lifecycle) → `status: ACTIVE` (DEPRECATED giữ nguyên); KHÔNG đụng docs/domain (ký riêng) + eng product (TRANSLATED→ENRICHED) + plans (PLANNED = đúng nghĩa sau duyệt). Gate mới **`doc_stamped`** @approve-document verify stamp đã xảy ra trên disk (mirror domain_stamped; TEMPLATE/README miễn; vacuous khi chưa có doc; force+audit). gates+approve_document selftest + smoke 36/36. |
| 2026-07-04 | domain_stamped (vá approve chay) | **Vá "approve rồi vẫn DRAFT" (bắt được khi user chạy thử):** 2 nguyên nhân — (1) tài liệu nói stamp `approved: true` trong khi script/gate dùng `status: APPROVED` (drift chữ nghĩa 10 file: command/skill/agent/build_prompt/state.py/STATE-MACHINE) → thống nhất hết về `status: APPROVED`; (2) gate `/domain-approve` chỉ check jargon, KHÔNG verify stamp đã xảy ra → MAIN complete được mà quên chạy `domain_approve.py` (file vẫn DRAFT, sau đó `domain_signed` fail khó hiểu). Thêm gate **`domain_stamped`**: doc thuộc target phải ĐÃ `status: APPROVED` trên disk lúc complete (target lẻ chỉ check doc đó; chưa author gì = vacuous; force+audit); build_prompt/command nhấn "BẮT BUỘC chạy script, không stamp tay". gates selftest (+4 case) + smoke 36/36. |
| 2026-07-04 | tách /design-ux (ux-designer) | **UX/UI tách khỏi solution-architect thành bước + agent chuyên môn riêng (mirror pattern PO/BA của DOMAIN):** lệnh mới `/design-ux` (DESIGN→DESIGN self-loop + back-edge PLAN→DESIGN) spawn `ux-designer-agent` (skill `ux-design` có sẵn) — own `ux/ux-*.md` + `ux/design-tokens.css`; solution-architect GỠ quyền UX (owned paths + forbidden + workflow: FE boundary chỉ HLD + đảm bảo BE contract đủ cho UX consume). Thứ tự: `/design` (chốt boundary + api) → `/design-ux` (consume contract, không bịa endpoint) → lặp xen kẽ → `/design-end` (gate không đổi — per-boundary ux + design-tokens vẫn đòi đủ). 23→24 command; +2 transition STATE-MACHINE; build_prompt +build_design_ux; E-6 keyword +design-ux; STAGE_NEXT_GUIDE DESIGN/PLAN cập nhật; technical-design + ux-design skill đổi mô tả vai; đồng bộ README/AGENTS/SETUP-GUIDE/PROTOCOL/commands+agents README. smoke +1 step (36/36). Trả lời câu hỏi PO/BA: đã tách chuẩn từ trước (domain-po/domain-ba agent + skill + owned paths riêng) — UX là chỗ gộp vai cuối cùng. |
| 2026-07-04 | doc-align flow PO/BA | **Đồng bộ TOÀN BỘ tài liệu theo flow sản xuất mới (quét stale 1 lượt):** `/domain-start` (đã bỏ) + "author thẳng vào docs/architecture, KHÔNG translate" (flow cũ) còn rải ở ~30 chỗ → thay bằng chuỗi `/domain-po`·`/domain-ba` → `/domain-approve` → `/domain-translate`. Sửa: policies (STAGE_NEXT_GUIDE + _BACK_HINT + selftest), gates/build_prompt/discovery_gate messages, STATE-MACHINE descriptions (DOMAIN 2 lớp + discovery tiến-wave qua start), 6 agent + agents/README (22 agent, +domain-translator row, apply-cr→DOMAIN), 7 command + commands/README (bảng 23 lệnh + design self-loop/design-end + back-edge), 4 skill discovery (Done → `/discovery-start D{N+1}` thay `/discovery-end <D>` đã bỏ arg) + boundary-charter + business-analysis, README/AGENTS.md/SETUP-GUIDE (23 command, flow 2 lớp, gate checklist mới), PROTOCOL diagram, TEMPLATE.CHARTER/cr/doc-review-findings/api (§7 breaking), done-wave-agent allowed-next (apply-cr chạy TỪ DONE trước done-wave; start-wave chỉ từ REVIEW). **+2 bug chức năng:** `reset_for_new_project.py` không dọn `docs/domain/**` + `translation-log/doc-review-findings/decisions.md` + `ux/design-tokens.css` khi reset project mới (giờ dọn đủ). Change-log/Removed giữ nguyên (lịch sử). Selftests + smoke 35/35 + state validate xanh. |
| 2026-07-02 | proof-lock + coverage-derive | **Vá 2 lỗ "agent tuân thủ" (audit enforcement):** (1) **FM-PROOF-FORGE** — `tracking/**` miễn phase-lock nên agent Write đè được `docker-ps/health-proof/api-proof.json` (fake cả tầng bằng-chứng-harness-đo); thêm `policies.is_proof_file` + deny ở `PreToolUse(Write\|Edit)`: 3 proof file CHỈ `capture_infra_proof.py` được sinh (tracking còn lại — report/bugs/registry — vẫn ghi tự do). (2) **coverage HARNESS derive** — `coverage_pct` trong `all_boundaries_reviewed` là số agent tự khai; giờ service đã scaffold → harness đọc report thật (jacoco XML `build/reports/jacoco/**`/`target/site/jacoco` · vitest/jest `coverage/coverage-summary.json` · lcov `coverage/lcov.info`): có report = số đo THẮNG số khai; scaffold mà không report = fail; chưa scaffold = fallback khai (hermetic/smoke giữ nguyên). Mirror triết lý derive test_result. policies+gates selftest (+proof-file & coverage-derive hermetic) + dispatcher + smoke 35/35. |
| 2026-07-02 | doc-gap P0 (khớp-nối + contract) | **6 gate khớp-nối chống tài-liệu rơi im lặng giữa các khâu (audit toàn tuyến ideation→test + trục contract):** (A) `translation_parity` @domain-end — business đã KÝ ↔ eng doc 1-1 qua `source`/`domain_source_id` (translate bỏ sót = chặn; eng epics/feat/BR mồ côi không source = chặn; translator phải dịch đủ 100%). (B) `todo_resolved` @design-end — marker `TODO engineer`/`TBD (DESIGN)`/`enforcement_location: TBD` translator để lại phải được DESIGN điền hết (BR không nơi enforce = rule không bao giờ được code, chỉ lộ ở UAT). (C) `plan_integrity` +chiều ngược **FEAT mồ côi**: FEAT-*.md không vào `features[]` boundary nào = không bao giờ được build/test = chặn (opt-out frontmatter `status: deferred\|dropped`). (E) `ac_coverage` @test-plan — traceability FEAT.AC ↔ TC 2 chiều bằng máy (parse `### AC-n` vs cột feature+AC registry): AC in-scope thiếu TC = mồ côi; TC trace AC không tồn tại = stale sau /apply-cr đổi AC; token deferred bỏ qua ("coverage matrix 100%" hết là lời hứa). (K1) `contract_graph_parity` @plan — đồ thị contract (api-*.md `consumers[]` + INTEG-INT consumer/producer + events subscriber/§9) ↔ MATRIX `depends_on`/`consumed_by` khớp 2 CHIỀU: id ma / cạnh contract không có trong MATRIX / cạnh MATRIX không contract doc nào ghi nhận = chặn (3 nguồn khai 1 sự thật). (K2) `api_contract_proof` @dev-handoff — `capture_infra_proof.py` mở rộng fetch OpenAPI runtime (`/v3/api-docs` springdoc, HARNESS đo) → `api-proof.json`; endpoint khai `api-{b}.md` (bảng `Method · Path`, normalize path-param) phải tồn tại trong spec runtime = bắt contract↔implementation drift (gốc BUG-006) TRƯỚC test; GraphQL/không api doc miễn; ref-backend-config ghi rõ springdoc bắt buộc. Tất cả force-bypass+audit; gates+planning_lint+capture selftest (6 cụm mới hermetic) + smoke 35/35. |
| 2026-07-02 | anti-fake-pass + UI-fidelity | **Vá pass-ảo test + nâng UI fidelity (2 gap e2e ghi nhận: UI-test luôn né được + registry over-scope).** (1) gate `ui_test_present` @test-plan: mỗi web boundary trong wave phải có ≥1 auto-TC UI in-scope (UI toàn manual/deferred/vắng = chặn — trước đây registry toàn API-TC vẫn `test_result=pass` dù FE chưa từng render). (2) `test_evidence` @test-execute nâng 3 lớp: **screenshot bắt buộc** cho TC web boundary pass|fail (`screenshots/{TC}*.png`, check PNG/JPEG magic-bytes ≥1KB — chống UI-test khống); **skip đối chiếu health-proof** (proof nói service UP → skip "service-down" bị chặn; chết thật → re-run capture_infra_proof.py); **siết skip-marker** thành cụm-từ cụ thể (bỏ "down"/"unavailable" đơn — trước match cả "dropdown"). (3) gate `registry_scope` @test-plan: auto-TC chỉ trace FEAT thuộc wave plan ≤ wave hiện tại (registry tích luỹ — FEAT cũ hợp lệ regression; FEAT wave TƯƠNG LAI = over-scope chặn) + FEAT/AC deferred mà row thiếu tag `@deferred` = chặn (vá "TC cho feature defer → bug rác chặn end-wave"). (4) `web_styling` @dev-handoff thêm lớp 3: dùng `var(--...)` mà token KHÔNG định nghĩa/import trong bundle (không copy/`@import` design-tokens.css, không `:root{--}`) = chặn (var resolve rỗng → UI vẫn trắng dù "dùng token"). (5) `design_gate`: có web boundary → `ux/design-tokens.css` phải tồn tại. (6) Skill uplift cho "UI đẹp": ux-design +§Visual polish (app shell / spacing rhythm / type scale / component primitives / interaction states / elevation — spec cụ thể đối chiếu được); rules-web rule 44 ép copy+import design-tokens vào bundle + UI primitives dùng chung đủ hover/focus; review-web §6 +token-định-nghĩa-trong-bundle (BLOCKER) + visual states; test-execute assert computed-style theo token (trang trắng/unstyled = FAIL+bug). gates selftest (3 case mới hermetic) + smoke 35/35 + policies/dispatcher/capture selftest + state validate xanh. |
| 2026-07-10 | W1 dedup router (Lost-in-the-Middle, L04) | **Thu gọn CLAUDE.md §RUNTIME GUARANTEES — bỏ trùng lặp với PROTOCOL.md (đối chiếu khóa Harness Engineering, bài L04 'one giant instruction file'):** dòng 145 cũ = 1 bullet ~4000 ký tự nhồi toàn bộ gate của mọi command (bản sao LỖI THỜI của PROTOCOL.md §Gate evidence — thiếu `docker_compose_ok`/`connectivity_ok` mà `gates.py GATE_RULES` test-plan có → drift). Thay 12 bullet dày bằng **bảng awareness 1-dòng-mỗi-hook + pointer** tới `harness/PROTOCOL.md §Hooks/§Gate evidence/§Turn-flag`; chi tiết sống ở PROTOCOL.md (SoT). Migrate cơ chế **turn-flag #11** (chỉ tồn tại ở CLAUDE.md) sang PROTOCOL.md §Turn-flag mới (reset/tiêu-cờ/gate-fail-không-tiêu/vá Skill|SlashCommand/#12) — không mất thông tin. Kết quả router: 25.110 → 17.781 ký tự (−29%, ~1.830 tokens/session). RỦI RO LOGIC = 0 (hook đọc gates.py/STATE-MACHINE.json, không đọc CLAUDE.md). |
| 2026-07-10 | W1 tiếp — rút gọn §SLASH COMMANDS | **Viết lại mục lệnh theo dạng 'lệnh gì · tác dụng gì · (chuyển stage)' — cắt đuôi gate lặp PROTOCOL.md:** ~6 dòng dày (dev-handoff/approve-document/design-end/plan/discovery-start) nhồi chi tiết gate (coverage BE80/jacoco, doc_stamped, ADR≥3...) = bản sao §Gate evidence → cắt, thay bằng pointer 1 dòng. State transition viết CHUẨN theo `STATE-MACHINE.json` (SoT) — **sửa lỗi cũ**: `/dev-handoff` thực tế `REVIEW_DEV→DEV_HANDOFF` (text cũ ghi nhầm `→TEST_PLAN`), `/test-plan` mới là `DEV_HANDOFF→TEST_PLAN`. Router (cả W1+phần này): 25.110 → ~15.1K ký tự (−40%). |
| 2026-07-10 | W1 chốt — SLASH COMMANDS dạng bảng + move changelog | **Theo góp ý user:** (1) viết lại §SLASH COMMANDS thành **bảng `Bước | Lệnh | Tác dụng`** xếp theo happy-path 8 bước (Khám phá→Yêu cầu→Thiết kế→Kế hoạch→Rà soát→Code→Test→Đóng + Ngoài luồng CR) — tiếng Việt dễ hiểu cho người mới, bỏ prose dài dòng + mã gate. (2) `git mv docs/HARNESS-CHANGELOG.md → HARNESS-CHANGELOG.md` (root, cùng thư mục CLAUDE.md) + cập nhật pointer. Router cuối: 25.110 → 14.745 ký tự (−41%). |
| 2026-07-10 | W2' dev WIP=1 per-feat (L07) | **Dev làm TUẦN TỰ từng feat thay vì cả boundary một lần (áp L07 WIP=1 đúng chỗ — implementation, khác /design):** `build_prompt.py` `_feat_task` (start-dev) đổi 'implement MỌI AC' → 'implement TUẦN TỰ từng FEAT một (feat1→feat2→feat3): mỗi feat xong hẳn (AC+BR+test XANH)→commit→feat kế; không mở song song, không bỏ dở; làm HẾT'. Đồng bộ 2 bản sao chống drift: `_template-dev-agent.md` (§Trách nhiệm 1 + workflow step) + `commands/start-dev.md` §Agent behavior. Enforcement = advisory-in-prompt + Stop hook build/test-green (không thêm state/gate → không drift, tôn trọng triết lý derive-don't-declare). **W2 gốc (thêm status field vào FEAT) BỎ** — FEAT đã machine-readable, thêm field tay sẽ drift. smoke 36/36. |
| 2026-07-10 | W5 executable arch rules (ArchUnit, L10) | **Kiến trúc layer từ 'review-agent đọc' → 'test chạy được chặn' (vá mâu thuẫn triết lý: harness đo-không-tin mọi chỗ TRỪ layer):** (1) gate `check_code_compliance` +check (e) — backend boundary scaffold phải có ≥1 ArchUnit test (`src/test/**/*.java` import `com.tngtech.archunit`); thiếu = BLOCKER (mirror Dockerfile-missing). PASS tự động do gradle/Stop hook chạy (ArchUnit = JUnit) → không thêm gate 'chạy', chỉ ép TỒN TẠI. (2) `ref-backend-pattern §7.5` — template ArchUnit (Layered: layeredArchitecture + ctrl-no-repo + @Entity ở entities/*Entity + no-cycle; Hexagonal: domain-pure + onionArchitecture) map 1-1 với §7 Forbidden. (3) `ref-backend-config` +dependency `archunit-junit5:1.3.0`. (4) `build_prompt` scaffold task (kind==backend) + `rules-backend` §1 ép scaffold gồm ArchitectureTest. (5) `review-backend §L` hạ phụ thuộc: layer do ArchUnit chặn máy, review lo semantic/borderline. Scope: intra-boundary backend; cross-boundary FK + web/mobile giữ nguyên. gates+build_prompt syntax OK, gates selftest + smoke 36/36. |
| 2026-07-10 | Vá khe ac_coverage (FEAT 0-AC lọt verify) | **Bug thật phát hiện khi user bắt 'tự tin quá':** `check_ac_coverage` cũ dùng `if not acs: continue` — gộp 2 case (FEAT không có file / FEAT có file nhưng 0 heading `### AC-n`) → FEAT malformed (0 AC) bị BỎ QUA IM LẶNG, không bị đòi TC, lọt verify tới end-wave (planning_lint chỉ ép epic_ref/feat_type, KHÔNG ép có AC — '≥4 AC' chỉ là lời khuyên skill). Sửa: tách `acs is None` (không file → plan_integrity lo) khỏi `not acs` (file tồn tại + 0 AC → FAIL, trừ khi deferred). +selftest case (e). Đóng khe VCR mà trước đó tôi claim nhầm là 'gate ép kín'. gates selftest + smoke 36/36. |
| 2026-07-10 | journey_e2e_present (e2e đa-hop, L10) | **Vá khe e2e journey đa-hop bị hoãn tới FE (user: framework phải cover flexible):** định nghĩa e2e cũ cột vào UI (§58 'CHỈ full-stack BE+FE') → wave backend-only chỉ được `contract_test_present` phủ PAIRWISE (A→B, B→C), luồng A→B→C chạy liền không bị ép test → bug seam (data transform tích lũy/state) lọt tới khi FE tới (đắt hơn, L10). Gate mới **`journey_e2e_present`** @test-plan: phát hiện chuỗi transitive `depends_on` ≥3 boundary (dùng data MATRIX sẵn có, KHÔNG parse JOURNEY vì journey là artifact nghiệp vụ không khai boundary) → đòi ≥1 auto-TC group=e2e|integration SPAN cả chuỗi (boundary/tags đủ mọi node). **API-driven curl là đủ — KHÔNG cần FE** → backend-only wave validate luồng đa-service sớm. Chuỗi ≤2 = vacuous (pairwise=full). Mirror `contract_test_present`. Selftest hermetic (order→payment→ledger: pairwise fail / span pass / 2-boundary vacuous / force). test-plan skill §pyramid+§quy-ước + PROTOCOL gate table cập nhật. gates selftest + smoke 36/36. |
| 2026-07-11 | feat-state (L05/L07/L08 bước 1) | **Feat-state machine — bước 1: DERIVE + clock-in (nền cho WIP=1-cứng).** Đối chiếu khóa harness-engineering: điểm mù lớn nhất của kernel là scope-discipline (L07 overreach) + continuity cấp-feat (L05/L08) — harness track ở cấp boundary/wave, KHÔNG có tầng feat-state. `gates.derive_feature_states` (ghép hàm có sẵn: `_feat_file_for`+`_AC_HEADING_RE` AC của FEAT · registry TC↔AC · `_report_results` TC→pass · `_wave_deferred_tokens`) → mỗi FEAT in-scope: `passing` (mọi AC in-scope có TC pass) / `active` (một phần) / `not_started` / `deferred` / `no-file`. Là VIEW (như derive_test_result), không phải file agent khai. `capture_feature_state.py` (MAIN chạy sau /test-execute) ghi `tracking/{wave}/feature-state.md`; PreToolUse chặn agent ghi tay (FM-PROOF-FORGE +feature-state.md). SessionStart brief đọc file → dòng `features = X/Y passing · đang dở: FEAT-...` = **clock-in L05** (session mới thấy ngay feat còn dở, không dò lại). Bước 2 (gate `features_complete` @end-wave: VCR=1.0, chặn ship khi feat nửa-vời) TÁCH riêng — để feat-state tự chứng minh 1 vòng thực tế trước khi treo gate ship. gates+capture+policies selftest (hermetic) + smoke 36/36 xanh. |
| 2026-07-11 | feat-state (L07 bước 2) — WIP=1 ship-gate | **Gate `features_complete` @end-wave — WIP=1 từ lời-dặn → enforcement máy (L07).** Sau bước 1 (derive + clock-in), treo gate ship lên feat-state: `check_features_complete` derive `feature-state.md`, chặn /end-wave nếu còn in-scope feat `active` (một phần AC verified, phần khác chưa = làm dở nửa chừng = overreach). Đây là VCR=1.0 tại điểm harness quan sát được (sau test, có report) — thay cho lời-dặn WIP=1 trong prompt W2'. **Kỷ luật chống chặn-oan:** CHỈ chặn `active`, KHÔNG chặn `not_started` (AC chỉ-manual chưa ghi report = not_started; test_passed/ac_coverage/uat_signed đã phủ). Không trùng test_passed (auto-TC pass) / ac_coverage (AC↔TC design-time) — cái này bắt "feat verify được NỬA" ở run-time. Lời-dặn WIP=1 (build_prompt dev) nối tới gate ("feat làm dở = wave không đóng được") thay vì gỡ. commit-per-feat signal @DEV: DEFER (git-style đa dạng, dễ false-positive, giá trị mờ). end-wave command/PROTOCOL/CLAUDE đồng bộ. gates selftest (+3 case: passing pass / active chặn / not_started không chặn) + smoke 36/36. |
